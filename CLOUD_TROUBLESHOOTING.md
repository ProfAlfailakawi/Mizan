# الوضع السحابي: لماذا يتعطّل، وكيف يُشخَّص

## المشكلة التي عولجت

كان كلُّ عطل سحابي يخرج بالجملة نفسها: **«تعذّرت المزامنة»**. وخلفها ستّ حلقات مختلفة تمامًا، كلٌّ منها يصلحها شخصٌ مختلف. فيقف صاحب المسابقة أمام جملةٍ لا تقول له أيّ حلقة انقطعت، ويجرّب بالتخمين.

الآن: **أدوات المؤسسات ← البنية الميدانية ← الاستمرارية ← «تشخيص السحابة»**. تفحص الحلقات بترتيبها وتقول أين انقطع الخيط ومن يصلحه.

## الحلقات الستّ

| # | الحلقة | متى تنقطع | من يصلحها |
|---|---|---|---|
| ١ | مفاتيح Firebase | التطبيق بُني بلا `VITE_FIREBASE_API_KEY` **أو** بلا `VITE_FIREBASE_PROJECT_ID` — والثانية لا توقف الإقلاع، فيبدو البناء سليمًا ولا يكتب شيئًا | من ينشر الخادم |
| ٢ | الاتصال | وضع الانقطاع مفعَّل يدويًا، أو الجهاز بلا شبكة | أنت |
| ٣ | جلسة الدخول | لا جلسة، أو انتهت | أنت |
| ٤ | **مطالبات الصلاحية** | رمز الدخول بلا `role` / `org_id` | زرّ «أعد كتابة مطالباتي» |
| ٥ | **كتابة المطالبات على الخادم** | ينقص `FIREBASE_PROJECT_ID` أو ADC بصلاحية Firebase Authentication Admin. تُفحص بمحاولة `setCustomUserClaims` نفسها على معرّفٍ عشوائي يُولَّد في كل مرة — لأن بناء كائن الإدارة لا يُثبت صلاحية، والقراءة صلاحية أخرى غير الكتابة | من ينشر الخادم |
| ٦ | نطاق المسابقة | التخويل على جهة أو مسابقة أخرى | مدير الجهة |

## السبب الأرجح عندك

الحلقتان ٤ و ٥ هما الأكثر وقوعًا وأقلّهما وضوحًا.

قواعد Firestore **لا تقرأ سجلّ هويات ميزان إطلاقًا** — تقرأ مطالبات رمز Firebase وحدها (`role`, `org_id`, `competition_id`). فحسابٌ مخوَّل في ميزان ورمزُه خالٍ من المطالبات يكون **مخوَّلًا في كل شاشة ومرفوضًا عند السحابة في آنٍ واحد**.

ومن يكتب تلك المطالبات هو الخادم عبر `setCustomUserClaims`، وهذا يشترط:

```
FIREBASE_PROJECT_ID=<معرّف المشروع>
```

مع **Application Default Credentials** لحساب الخدمة، وعليه دور **Firebase Authentication Admin**.

إن نقص أيٌّ من هذين: لن تُكتب المطالبات لأي حساب، ولن يُصلح زرُّ الإصلاح شيئًا مهما ضُغط، ولن يعمل الوضع السحابي لأحد. الشاشة تقول ذلك الآن صراحةً بدل أن تصمت.

### الفحص السريع

```bash
curl -s https://<عنوان-النشر>/api/health | jq '{firebaseProjectConfigured, identityClaimsWritable, identityGovernanceConfigured}'
```

- `true` ⇒ الخادم يكتب المطالبات فعلًا — مُثبتًا بمحاولة كتابة حقيقية لا باستنتاج.
- `false` ⇒ العلّة في النشر (الحلقة ٥). اضبط المتغيّر والاعتماد ثم أعد النشر.
- `null` ⇒ **لم يحسم الفحص** (شبكة، أو حصّة، أو رمز خطأ غير مصنَّف). ليست عجزًا: أعد المحاولة.

وإذا كان `true` وما زال الرفع يفشل ⇒ افتح «تشخيص السحابة» واضغط **«أعد كتابة مطالباتي»**.

> الزرّ لا يظهر حين يكون `false`: ضغطُه حينها يعود بـ `IDENTITY_CLAIMS_NOT_CONFIGURED` حتمًا، فيُشغل عن العلّة الحقيقية في النشر. ويبقى معروضًا حين يكون `null`، لأن فحصًا لم يحسم ليس رفضًا.
>
> والنتيجة القاطعة وحدها تُخزَّن خمس دقائق، والعارضة يُعاد فحصها؛ والنداء الجاري يُشارَك بين الطلبات المتزامنة فلا يُكلَّف فحصٌ مخوَّل لكل استدعاء.

## زرّ «أعد كتابة مطالباتي»

لا يمنح صلاحية ولا يقبل دورًا من العميل. يطلب من الخادم إعادة كتابة المطالبات **من التخويل الموجود أصلًا** في سجلّ الهويات (`POST /api/identity/refresh-claims`)، ثم يُجدّد الرمز قسرًا — لأن المطالبات الجديدة لا تصل القواعد قبل رمزٍ يحملها.

رموز الفشل التي صارت تظهر بنصّها بدل أن تُبتلع:

| الرمز | المعنى |
|---|---|
| `ACCOUNT_NOT_PROVISIONED` | لا تخويل لهذا الحساب في السجلّ — مدير الجهة يدعوه أولًا |
| `IDENTITY_CLAIMS_NOT_CONFIGURED` | الخادم لا يستطيع الكتابة (الحلقة ٥) |
| `IDENTITY_GOVERNANCE_NOT_CONFIGURED` | حوكمة الهويات غير مهيَّأة على هذا الخادم |
| `IDENTITY_CLAIMS_SYNC_FAILED` | رفض Firebase الكتابة، والسبب مرفق |

## الخصوصية

لا تُعرض في الشاشة أي مفاتيح ولا أسرار — أسماء الحقول وحالتها فقط (`role` موجود/غائب، `org_id` موجود/غائب).

## الملفات

- `src/lib/cloud-diagnosis.ts` — منطق التشخيص نقيًّا ومختبَرًا (`tests/cloud-diagnosis.test.ts`).
- `src/components/admin/CloudDiagnostics.tsx` — الشاشة.
- `src/lib/store.ts` — `repairIdentityClaimsNow` يحفظ سبب الخادم بدل أن يعيد `false` مجرَّدة.
- `server.ts` — `/api/health` يعلن `identityClaimsWritable`.
- `server/firebase-claims.ts` — جسر المطالبات، وفحص الصلاحية بمحاولة كتابةٍ على معرّف عشوائي، بتصنيفٍ برموزٍ منصوصة وتخزينٍ للقاطع وحده.
