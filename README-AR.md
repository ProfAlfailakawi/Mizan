# حزمة ملاحظات Mizan — 2026-09-14

هذه حزمة تسليم مركّزة للملاحظات التي جُمعت في المحادثة. ليست مشروع Mizan كاملًا، ولا تحتوي مجلد wrapper عند فك الـ ZIP، ولا ترفع شيئًا إلى GitHub.

## المرجع الذي تم فحصه

- المستودع: `ProfAlfailakawi/Mizan`
- رأس `main` وقت الفحص: `4e234815a95ee6fa8d0b3a212898ad585251a463`
- التغييرات الأساسية للملاحظات موجودة في السلف: `00ccc3a58165908127525a6852b3c8337f67ba27`
- الأساس السابق لها: `21eeacd7dc14f0bdd415a912b0fc032881d2482f`

## ما تم التأكد منه

1. التسجيل لا ينتقل من خطوة المشاركة قبل اختيار الرواية المعتمدة للفئة، والخادم يرفض الرواية الفارغة أو المخالفة.
2. شاشة المشاركين تدعم فلتر الفئة، تحديد الصفوف، اختيار كل الظاهر، واعتماد المحدد فقط.
3. شروط المسابقة تعرض اختيارًا واضحًا بين الاعتماد التلقائي والمراجعة قبل الاعتماد، والخادم يطبّق السياسة.
4. حذف المسابقة يظهر فقط للمسابقة الخالية من المتسابقين، والخادم يعيد التحقق من Firestore قبل الحذف ويمنعه عند وجود أي متسابق.
5. البريد في شاشة تفعيل الحساب يُعرض LTR مع عزل BiDi داخل الواجهة العربية.
6. حقل «الأتمتة» المربك أزيل من بيانات المسابقة، مع إبقاء السلوك الداخلي الافتراضي آليًا.
7. خيار «المتسابق يختار نطاقه» أزيل من الواجهة والمسار العام؛ نطاق الأسئلة مصدره الفئة.

## الملفات في هذه الحزمة

- `apply-user-notes.sh`: يطبّق فقط ملفات هذه الملاحظات من commit المحلي المعروف إذا كانت غير موجودة. لا يعمل commit ولا push.
- `verify-user-notes.mjs`: فحص مستقل للنقاط السبع من ملفات المشروع.
- `user-notes-2026-09-14-regression.test.ts`: اختبار regression الموجود في المصدر الحالي كمرجع إضافي.
- `SOURCE-MANIFEST.md`: الملفات المتأثرة والحدود المقصودة للحزمة.

## الاستخدام

من أي مكان:

```bash
bash apply-user-notes.sh /path/to/Mizan
```

إذا كانت النسخة الحالية هي `main` أو تحتوي commit الإصلاح أصلًا، فلن يغيّر السكربت أي ملف وسيشغّل التحقق فقط.

إذا كانت النسخة عند الأساس السابق وتملك كائنات Git للـ commits المشار إليها، فسيولّد الفرق محليًا ويطبقه على الملفات المقصودة فقط بعد `git apply --check`. يتوقف إذا كانت الملفات المستهدفة معدلة محليًا حتى لا يطغى على عمل آخر.

للتحقق فقط:

```bash
node verify-user-notes.mjs /path/to/Mizan
```

## حدود مقصودة

الحزمة لا تضم إصلاحات الكاميرا/QR أو إضافة `jsqr` الموجودة أيضًا في commit `00ccc3a`؛ هذه الحزمة تخص الملاحظات السبع أعلاه فقط. ولا تجري أي كتابة على GitHub أو أي نشر للموقع.
