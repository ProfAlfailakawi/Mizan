# محرك النطاق والأسئلة — Mizan Quran Competition Intelligence Engine

## القواعد الذهبية

> **A competition category is data + scope + rules, not a hard-coded competition type.**
> الفئة في ميزان تُعرّف بنطاقها وقواعدها الفعلية، لا باسمٍ ثابت داخل الكود.

> **Participant Effective Scope is the authoritative source for question eligibility whenever
> participant-specific selection applies.**
> متى كان للمتسابق نطاقٌ معتمد فهو وحده مرجع أهلية السؤال.

> **Mizan does not promise zero repetition when mathematically impossible. It minimizes
> repetition, maximizes separation, balances reuse, and records the reason.**
> ميزان لا يَعِد بعدم التكرار حين يستحيل رياضيًا؛ يُقلّله، ويُباعد، ويوازن، ويسجّل السبب.

لا يوجد في الكود فرعٌ شرطي واحد يتصرّف بحسب اسم الفئة. لا `if (category === 'full_quran')`،
ولا `if (name.includes('ربع'))`. الاسم للإنسان، والنطاق للنظام.

## البنية

| الوحدة | المسؤولية |
|---|---|
| `src/lib/quran-canon.ts` | المرجع القانوني لبنية المصحف: عدد آيات كل سورة، مطالع الأجزاء الثلاثين، أسماء السور، مطالع الصفحات. كل وحدة أخرى مشتقة منه بدرجة توثيق معلنة. |
| `src/lib/quran-scope.ts` | `QuranScope` — قائمة مقاطع من `surah:ayah` إلى `surah:ayah`، مع تطبيع ودمج وجبر مجموعات وقياس وبصمة. **المرجع الوحيد لمعنى «أين يجوز طرح السؤال؟»** |
| `src/lib/participant-scope.ts` | قواعد اختيار المتسابق ودورة حياة نطاقه ونسخه. |
| `src/lib/question-zones.ts` | مناطق التوزيع، والقسمة المتوازنة، وقصّ المنطقة على نطاق المتسابق. |
| `src/lib/repeat-policy.ts` | سياسة التكرار، ورتب القيود (MUST / PREFER)، والحدّ الأدنى النظري للتكرار. |
| `src/lib/question-engine.ts` | المحرك: مرشّحات قاطعة ثم مفاضلة، مع سبب بنيوي لكل اختيار. |
| `src/lib/scope-demand.ts` | تحليل الطلب والعرض، وخريطة الازدحام، وضغط الندرة، والحدّ الأدنى المراعي للمناطق، والتوصيات. |
| `src/lib/model-fairness.ts` | عدالة النموذج الكلية، وإبطال ما بُني على نسخة نطاق منتهية. |
| `src/lib/competition-twin.ts` | التوأم الرقمي: تشغيل المسابقة افتراضيًا بالمحرك نفسه. |
| `src/lib/scope-migration.ts` | ترحيل `juzCount` / `memorizationScope` بلا اختراع نطاق. |
| `src/lib/scope-engine.ts` | الواجهة التي يمرّ منها المخزن والخادم والشاشات. |
| `src/lib/scope-readiness.ts` | فحص الجاهزية الخاص بالمحرك. |

## درجات التوثيق (assurance)

| الدرجة | المعنى | الوحدات |
|---|---|---|
| `CANONICAL_TABLE` | جدول قانوني ثابت | السور، الآيات، الأجزاء الثلاثون |
| `DERIVED_PROPORTIONAL` | مشتق تناسبيًا داخل الوحدة الأكبر، ويُعلن ذلك في الواجهة | الأحزاب، الأرباع، الصفحات |
| `CERTIFIED_SOURCE` | من حزمة المصدر المعتمدة على الخادم | النص، الصفحات الحقيقية، الصعوبة المراجَعة |

حزمة المجمع المستعملة لا تحمل حقلي الحزب والربع، فلا يُختلق لهما جدول. تُشتق حدودهما داخل
الجزء بقسمة آياته، ويُعلَن ذلك صراحةً، ويبقى المنظم قادرًا على تحريك الحدّ إلى آية بعينها.

## أسبقية عدد الأسئلة (معلنة، لا صامتة)

1. `category.questionsCount` إن كان أكبر من صفر.
2. وإلا `policy.questions.questionsPerParticipant`.
3. وإلا واحد.

مجموع أسئلة المناطق **لا** يتقدّم على هذا العدد؛ مخالفته خطأ تهيئة يظهر في فحص الجاهزية.
وعدد الأسئلة مستقل تمامًا عن حجم النطاق في الاتجاهين.

## رتب القيود

**MUST** — فشله يُبطل السؤال: نطاق المتسابق · سياق القراءة · المنطقة · اعتماد المصدر ·
حالة السؤال · عدم التكرار داخل النموذج · عدم إعادة الموضع للمتسابق نفسه · سقف الاستعمال ·
الجوار داخل النموذج الواحد.

**PREFER** — يُحاوَل ويُسجَّل سبب التنازل: تنوّع السور · التباعد الزمني والمكاني ·
قلّة الانكشاف · حماية المواضع النادرة للقادمين · توازن الاستعمال.

## حاجزان للنطاق، لا حاجز واحد

1. **البنك يُقصّ** على نطاق المتسابق قبل أن يصل إلى السحب (`store.startSessionForParticipant`).
2. **الخادم يُعيد التحقق** لحظة الكشف (`SecureQuestionRuntimeRepository.revealQuestion`)،
   فيرفض `QUESTION_RUNTIME_SCOPE_VIOLATION` ولو تلاعب أحدٌ بالبنك بعد التخصيص.

ولتدقيقٍ لا يكشف السؤال: `GET /api/enterprise/question-runtime/:sessionId/scope-verification`.

## الحدّ الأدنى الحقيقي للتكرار

القسمة الساذجة (سحوبات ÷ مواضع) تُجمّل الصورة لأنها تفترض أن كل موضع يصلح لكل سحبة.
`zoneAwareReuseLowerBound` يبني «مجموعات طلب» = (عنقود نطاق × منطقة)، ثم يأخذ أكبر
(طلبٌ محبوس ÷ مواضع متاحة) على المجموعات واتحاداتها الثنائية. الفرق بين المحقَّق وهذا الحدّ
هو التكرار الذي سببته الخوارزمية وحدها.

## التشغيل

```bash
npm run fairness:lab                      # ألف · خمسة آلاف · سيناريو العجز
npm run fairness:lab -- --large=10000     # ويُضاف إليها عشرة آلاف
npm test                                  # كل الاختبارات
```

## مجموعات الاختبار

`quran-scope-engine` · `participant-effective-scope` · `question-zone` ·
`question-repeat-policy` · `scope-scarcity` · `model-fairness` · `scope-simulation` ·
`scope-migration` · `scope-server-enforcement` · `scope-end-to-end`

## ما لا يدّعيه هذا المحرك

* لا يدّعي حكمًا شرعيًا في الابتداء: البداية دائمًا عند حدّ آية حقيقي، والوصف بنيوي بحت.
* لا يسمّي تقريره «شهادة علمية»: اسمه *تقرير عدالة وتوزيع الأسئلة*.
* لا يُسلّم سؤالًا إلى لجنة تحكيم رسمية إلا من مصدر معتمد عبر خزنة السؤال؛ والمواضع
  البنيوية المسقطة من جدول المصحف تُستعمل لقياس السعة والندرة والمحاكاة وحدها.
* لا يخترع نطاقًا من بيانات قديمة مبهمة: يعلّمها `needs_scope_confirmation` ويعرض اقتراحًا
  لا يُطبَّق حتى يعتمده المنظم.
