# ملاحظات المستخدم — ١٥ سبتمبر ٢٠٢٦

ما نُفِّذ من ملاحظات صاحب المسابقة، وما بقي، وأين يُقرأ كلٌّ منها في الكود.
الاختبارات كلها في `tests/user-notes-2026-09-15.test.ts` — اختبارٌ لكل ملاحظة.

| # | الملاحظة | ما صار | الموضع |
|---|---|---|---|
| ١ | شاشة التحكيم فارغة رغم وجود طلبة | الشاشة تقول سبب الفراغ (كم مسجَّلًا، كم في الطابور، كم لم يُسجَّل حضوره، كم تحت المراجعة)، وتفتح للمحكّم إدخال الحاضر أمامه إلى الطابور ونداءه — ويُسجَّل باسمه في التدقيق | `src/components/judge/JudgeOS.tsx` |
| ٢ | ‎3.5000000000000004 | كل خطوة كسرية تُقرَّب إلى دقّة خطوتها قبل العرض | `src/components/admin/CompetitionOverview.tsx` (`quantize`) |
| ٣ | «هامش الصعوبة» غير مفهوم | صار «هامش الصعوبة المسموح» وتحته شرحٌ يحسب المدى المقبول بالأرقام الحالية | نفس الملف |
| ٤ | لا يمكن إضافة معيار أو درجة | **السبب**: لائحة المسابقة مجمَّدة، و`updateRuleSet` يردّ `false` وكانت الشاشة تتجاهله فيبدو الزرّ معطلًا. التجميد يبقى (الدرجة لا تتغيّر بعد بدء التحكيم) لكنه صار ناطقًا: شريطٌ يقول السبب وكيف يُرفع، والزرّ معطَّل ظاهرًا لا ميتًا | `src/components/admin/CompetitionOverview.tsx` |
| ٥ | «الدولي» → «الجهات» | التبويب والعناوين تتحدث عن الجهات المشاركة، محليةً كانت أو خارجية | `src/components/admin/EnterpriseWorkspace.tsx` |
| ٦ | بطاقة «النماذج الاحتياطية» غامضة وزرّها لا يعمل | صارت «نموذج بديل جاهز» بنصٍّ يقول ماذا يحدث بدونه وماذا يفعل الزر، والزر يُسمّى «تجهيز البدائل» ويفتح «النماذج والعدالة» | `src/lib/scope-readiness.ts` |
| ٧ | حذف «اشترط مراجعة علمية» | حُذف الخيار من الشاشة | `src/components/admin/QuestionEngineWorkspace.tsx` |
| ٨ | لا يمكن إضافة منطقة | المنطقة الجديدة تولد بنطاقٍ حقيقي (ما لم تغطّه المناطق)، وأي تعديل يُخرج الخطة من القسمة التلقائية إلى «مناطق تحددها اللجنة» فلا يُهمل، وزرّ «وزّع الأعداد» يُطابق المجموع مع عدد أسئلة المتسابق | نفس الملف |
| ٩ | ترتيب الأسئلة للمتسابق | العرض يتبع ترتيب المصحف تصاعديًا؛ السحب وإثباته لم يتغيّرا | `src/lib/question-engine.ts`, `src/lib/fairdraw.ts` |
| ١٠ | إضافة الحزب والسور | شبكة الأحزاب الستين صارت وحدة تحديد كاملة إلى جانب الأجزاء، والسور كانت موجودة | `src/components/scope/QuranScopePicker.tsx` |
| ١١ | حذف الآيات — الأوجه فقط | طول مقطع السؤال يُقاس بالأوجه وأرباعها فقط | `src/components/admin/CompetitionOverview.tsx` |
| ١٢ | سجل الفائزين التاريخي | الصفحة العامة تعرض الدورات السابقة بفائزيها ونِسَبهم، والمركز المحجوب يُذكر محجوبًا | `src/lib/winners-archive.ts`, `src/components/public/CompetitionLanding.tsx` |
| ١٣ | تطوير «الإحماء» | صار «التهيئة قبل دورك»: أين أُختبر، نفَسي، بروفة مؤقّتة يرصد فيها زلّاته بأزرار المحكّم نفسها — تمرينٌ لا يُسجَّل ولا يصل اللجنة | `src/components/participant/WarmupSanctuary.tsx` |
| ١٤ | قطر الأجزاء | `neighborhoodJuzRadius` في سياسة التكرار، وضابطه في الشاشة | `src/lib/repeat-policy.ts`, `src/lib/question-engine.ts` |
| ١٥ | المراكز بالنِّسَب والحجب | محرك مراكز مستقل: عتبة لكل مركز، وحجبٌ صريح، و«ضمن المنافسة» بنسبته في صفحة المتسابق | `src/lib/award-places.ts` |
| ١٦ | تنويع مواضيع الأسئلة | وسمٌ يُشتق من الموضع (أحكام، قصص الأنبياء، آيات قصيرة/طوال، مطالع، خواتيم) ويُستعمل تفضيلًا في الترجيح | `src/lib/question-topics.ts` |
| ١٧ | حفص افتراضًا وحذف ما سواه | الرواية اختيارٌ من جدول لا نصٌّ حر؛ حفص عن عاصم هو الافتراضي الوحيد | `src/lib/quran-reading-sources.ts` |
| ١٨ | بقية الروايات من موقع الوحي | الجدول يسجّل `alwa7y.com/downloads` مصدرًا معتمدًا لبقية الروايات؛ وتبقى كل رواية معطَّلة حتى تصل حزمتها | نفس الملف |

## ما بقي

**الملاحظة ١ — الوضع السحابي.** الجزء الخاص بالتحكيم عُولج أعلاه. أما أعطال السحابة نفسها فهي تشغيلية (مفاتيح Firebase، الصلاحيات، حجم الحمولة) ولا تُصلَح من الكود بلا رسالة الخطأ الفعلية: الرسائل مبنيّة أصلًا في `src/lib/store.ts` (`CLOUD_PAYLOAD_TOO_LARGE`, `CLOUD_PERMISSION_DENIED`). المطلوب لقطة من الشاشة عند وقوع العطل أو نصّ الرسالة.

## مصادر الروايات

- **حفص عن عاصم** — مجمع الملك فهد لطباعة المصحف الشريف. جاهز.
- **بقية الروايات** — موقع الوحي، <https://www.alwa7y.com/downloads/>. مسجَّلة في الجدول ومعطَّلة حتى تُسحب حزمتها وتُختم بمعالمها (السلطة، الإصدار، البصمة) كما تفعل حزم المجمع.

قبل السحب الفعلي من الموقع تُراجَع شروط استخدامه ورخصة نشره، ويُسجَّل ذلك في `SOURCE-MANIFEST.md` مع بقية المصادر.
