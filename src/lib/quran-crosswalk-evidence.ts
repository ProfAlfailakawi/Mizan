/*
 * صفوف جسر المواضع المعتمدة من اللجنة — الملف الذي يُملأ، لا يُخمَّن.
 *
 * ستّ روايات نصُّها حاضرٌ ومُتحقَّقُ البصمة، لكن عدّ آياتها يخالف العدّ القانوني في سورٍ
 * معلومة بالاسم. والانتقال بين العدَّين لا يُشتقّ آليًّا بيقين: حاولتُ محاذاة النصّين حرفًا
 * بحرف بعد تجريد الضبط، فاختلفا في مواضع رسمٍ حقيقية وفي عدّ البسملة نفسها — فالمحاذاة
 * تعطي **اقتراحًا** لا يقينًا، واقتراحٌ يُسأل عليه متسابقٌ في مسابقةٍ رسمية ليس مقبولًا.
 *
 * فالصفوف هنا تُملأ من قرار اللجنة وحده، ولكلّ صفٍّ غير تطابقي مرجعٌ منصوص (يرفضه
 * `validateCrosswalkRow` بدونه). وما دام الملف فارغًا تبقى تلك السور `UNRESOLVED`،
 * ويمنع فحصُ ما قبل الانطلاق فئةً عليها، ويُسمّى السبب بسورته.
 *
 * كيف يُملأ:
 *   1. `npm run quran:release-matrix -- --worklist`  → قائمة السور المطلوب حسمها لكل رواية.
 *   2. تحسم اللجنة حدَّ كل آية في السور المذكورة بمرجعٍ منصوص.
 *   3. تُضاف الصفوف هنا بمرجعها، فتنتقل الرواية تلقائيًا إلى «جاهزة للسؤال» بلا تعديل منطق.
 *
 * وحدة طرفية نقيّة: بيانات فقط، تُقرأ في المتصفّح والخادم معًا.
 */

import type { QuranLocusCrosswalk } from './quran-locus-crosswalk';

/** إصدار جدول الجسر — يُسجَّل في الجلسة لإعادة تفسير النتيجة تاريخيًا. */
export const COMMITTEE_CROSSWALK_VERSION = 'mizan-crosswalk-committee-v0-empty';

/**
 * صفوف اللجنة. فارغةٌ قصدًا حتى تصل قرارات حدود الآيات للسور المختلِفة عدًّا.
 * لا يُضاف صفٌّ هنا من محاذاةٍ آلية ولا من مرجعٍ غير منصوص.
 */
export const COMMITTEE_CROSSWALK_ROWS: readonly QuranLocusCrosswalk[] = [];
