/*
 * ارتفاعُ شريط بيئة العرض، والحشوةُ التي تُحجز له أسفل كل صفحة — رقمٌ واحد لا اثنان.
 *
 * كانت الحشوة `pb-28` في `App.tsx` وارتفاعُ الشريط في `DemoBar.tsx`، فكلُّ تغييرٍ في
 * أحدهما يترك الآخر خلفه: إمّا فجوةٌ فارغة أسفل كل صفحة، أو شريطٌ يغطّي آخر صفٍّ في
 * جدول. فصار المصدر واحدًا يقرؤه الاثنان.
 *
 * وهو ملفٌّ بلا واجهةٍ ولا متجر عن قصد، ليُقاس بلا تهيئةِ خادمٍ ولا مفاتيح.
 */
export const DEMO_BAR_HEIGHT_REM = 3.5;
export const DEMO_BAR_SHELL_PADDING = `${DEMO_BAR_HEIGHT_REM}rem`;
