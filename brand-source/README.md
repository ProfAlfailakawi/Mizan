# مصادر العلامة (غير مخدومة للمتصفح)

هذه ملفات مرجعية عمدًا **خارج** `public/` حتى لا تُشحن مع التطبيق.

- `mizan-logo-approved.png` — الشعار المعتمد الأصلي. هو المرجع الذي بُنيت منه
  `src/components/design-system/MizanLogo.tsx` و`public/brand/mizan-mark.svg`.
- `mizan-splash.webp` — شاشة البدء القديمة. **محفوظة للأرشيف فقط**: نصها العربي
  المطبوع داخل البكسل به خطآن إملائيان («المسايقات القرآئية»)، وقد استُبدلت بشاشة
  بدء متجهة نصُّها قابل للتصحيح.

مجموعهما ٤١٠ كيلوبايت لم تعد تُحمَّل على أي جهاز.

- `mizan-og.svg` — مصدر بطاقة المشاركة؛ تُصدَّر إلى `public/brand/mizan-og.jpg`.
- `mizan-icon-maskable.svg` — مصدر أيقونة PWA القابلة للقص؛ تُصدَّر إلى
  `public/brand/mizan-icon-maskable-512.png`.
