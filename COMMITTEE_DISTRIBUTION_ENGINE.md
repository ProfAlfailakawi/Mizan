# محرك التوزيع والشاشات — Committee Distribution & Venue Displays

> وثيقة تصميم. القسم **«اليوم»** يصف السلوك القائم فعلًا في الكود مع مراجعه، والقسم
> **«المقترح»** يصف التصميم. كل ادعاء عن السلوك الحالي مرفقٌ بملفه وسطره ليُتحقق منه.

## حالة التنفيذ

| الدفعة | الحالة |
|---|---|
| ١ · `display-board.ts` — الإسقاط النقي | ✅ منفَّذة (`src/lib/display-board.ts` + `tests/display-board.test.ts`) |
| ٢ · شاشة اللجنة + مسار `#board` | ✅ منفَّذة (`src/components/public/CommitteeDisplay.tsx`) |
| ٣ · شبكة القاعة لعشر لجان | ✅ منفَّذة (`src/components/public/WaitingBoard.tsx`) |
| ٤ · `public_boards` + القواعد + الناشر | ✅ منفَّذة (`firestore.rules`، `use-board-publisher.ts`، `parseDisplayBoard`) |
| ٥ · إصلاح التوزيع (الدقائق، الاحتياطي، الرواية) | ✅ منفَّذة (`arrival-core.ts`، `store.ts`، `OperationsPolicy`) |
| ٦ · الموجات + قيود العدالة + قرعة التعادل | ✅ منفَّذة (`distribution-plan.ts`، `WaveDistribution.tsx`) |
| ٧ · عدالة الانتظار عند النقل + الاستثناء عبر الفئات | ✅ منفَّذة (`queue-equity.ts`، وسمُ الاستثناء في JudgeOS) |
| ٨ · قياس الشاشات على أبعادٍ حقيقية | ✅ منفَّذة (`npm run qa:venue-legibility`) |
| ٩ · تعلّم إيقاع اللجنة من الواقع | ✅ منفَّذة (`session-tempo.ts` + `tempoMinutes` في المخزن) |
| ١٠ · محاسبة التقدير ومراجعة اليوم | ✅ منفَّذة (`day-retrospective.ts`، `DayRetrospective.tsx`) |
| ١١ · إغلاق فجوة التركيبة (استثناء الفئة في الموجة) | ✅ منفَّذة (`distribution-plan.ts`، خانة الإذن في `WaveDistribution.tsx`) |
| ١٢ · صمود النشر: تناوبٌ بين أجهزة الإدارة | ✅ منفَّذة (`board-lease.ts`، `use-board-publisher.ts`، `HallScreenPublisher.tsx`) |

قرارات الجهة المحسومة حتى الآن: **الخصوصية = أكواد فقط**، و**شاشة اللجنة تعرض التالي**،
و**هوية الفئة تظهر على كل شاشة**. وما بقي في الجزء الرابع أدناه لم يُحسم بعد.

**ما أضافته الدفعات ٩–١٢:**

- **إيقاع اللجنة يُقاس ولا يُفترض.** `averageSessionMinutes` كان يُكتب مرّة عند إنشاء اللجنة
  ولا يتغيّر، ويُقرأ في كل موضعٍ يمسّ زمن المتسابق — فلجنةٌ قُدّرت بثماني دقائق وهي تأخذ
  خمس عشرة كانت تُنتج أرقامًا خاطئة على ثلاث شاشات وفي حساب الحِمل وفي خطة الموجة معًا.
  صار الإيقاع **وسيطَ** آخر ثماني جلسات (لا متوسّطها: عطلُ صوتٍ نصفَ ساعة يُفسد المتوسّط
  ولا يحرّك الوسيط)، ولا يُستعمل قبل خمس عيّنات — فتقديرٌ ضعيف أشرف من تعلّمٍ من جلستين.

- **التقدير يُحاسَب.** كان الشيء الوحيد في المنصّة الذي يَعِد ولا يُراجَع. صار ما وُعد به
  المنتظر يُقارَن بما انتظره فعلًا: خطأٌ وسيطيّ ومتوسّط وميلٌ باتّجاه — ورقمٌ يُخلف يُفقد
  الثقة في كل رقمٍ بعده.

- **مراجعة اليوم.** المنصّة كلها لليوم نفسه، ولا شيء فيها ينظر إلى الوراء؛ فتُغلق المسابقة
  ويُعاد إعدادها في السنة القادمة بالأرقام نفسها التي ثبت خطؤها. التقرير **وصفيّ لا حافز**
  ويعلن ذلك في بنيته (`nonRanking`) — لأن ترتيب اللجان بالسرعة محظورٌ في الجزء الخامس.

- **فجوة التركيبة أُغلقت.** «عدالة الطابور» تنقل بين لجنتين وتلزمها لجنةُ مصدر، ومن لا
  تؤهّله أيُّ لجنة بلا لجنة أصلًا — فلم يكن له بابٌ إليها. صار للموجة إذنٌ صريح يُطلب
  (`allowCategoryException`): يُسنَد إلى أقلّ اللجان المتاحة حِملًا، ويُوسَم صفُّه فتعلم
  اللجنة أنها تحكم من ليس من فئتها، ويبقى **يُسأل في نطاق فئته هو** لا في تخصّص اللجنة.
  وبدون الإذن يبقى بلا لجنة ويُقال السبب — اعترافٌ بالعجز أشرف من إسنادٍ خاطئ بصمت.

- **النشر لم يعد معلّقًا بتبويبٍ واحد.** كان إغلاق تبويب الإدارة يوقف عشر شاشاتٍ معًا،
  والشاشات تقول الصدق ولا أحد في غرفة العمليات يعرف أن السبب عنده. صار كلُّ جهاز إدارةٍ
  حاضر مرشَّحًا: واحدٌ ينشر والبقيّة تراقب صامتة، فإن جمد ختمُ الناشر تولّى أقربُها —
  وميزانية التناوب مقيسة لتقع **تحت** الحدّ الذي تُعلن عنده الشاشة تأخّرها، فلا تومض
  القاعة عند كل تناوبٍ عادي. والعقد يُقاس بساعةٍ واحدة: الختم يُقارَن نصًّا لا يُفسَّر
  زمنًا، وإلّا كفى تأخُّرُ ساعةِ جهازٍ دقيقةً ليتخاطف الجميع العقد بلا توقّف.
  ولم يُحلّ هذا بخادمٍ يقرأ سجلّ المتسابقين: ذاك يعيد فتح الامتياز الذي أُغلق في الدفعة
  الرابعة، ولا يرى زمن الجلسة الجارية أصلًا.

**ما أضافته الدفعة الرابعة على ما وُصف أدناه:** الشاشة صارت **بلا امتياز** — تقرأ
`public_boards/{competitionId}` بلا حساب، وينشرها جهاز الإدارة. والقراءة لا تُصدَّق كما
جاءت: `parseDisplayBoard` يعيد بناء الإسقاط من الحقول المعروفة وحدها، فحقلٌ لم يُصمَّم —
اسمًا أو سؤالًا أو درجة — لا يبلغ الشاشة ولو كُتب في الوثيقة. والنشر على إيقاعين: فورًا عند
تغيّر المعروض، ونبضةٌ هادئة تبقي الختم الزمني حيًّا — وإلّا أعلنت قاعةٌ هادئة توقّفًا لا وجود له.

> **ما صار إليه التوزيع.** ملاحظات القسم ١٫٥ عولجت كلها: الحِمل يُقاس بالدقائق، والاحتياطي
> المخترق لشرط الفئة صار خيارًا صريحًا (`ANY_AVAILABLE`) بعد أن كان السلوك الوحيد، وأهلية
> الرواية تُفحص عند البوابة، وفضّ التعادل حتميّ. ومَن لا تؤهّله أيُّ لجنة يدخل الطابور برقمه
> بلا إسناد وتُفتح له حادثة — الشاشة تعدّه، والموجة توزّعه.
>
> **وعطبٌ حقيقي أمسكه القياس:** سقف حصّة الوفد كان يُحسب نسبةً خامًا، فحصّة أوّل واصلٍ على
> لجنةٍ فارغة مئةٌ بالمئة مهما كان وفده — فيُمنع من كل لجنةٍ خفيفة ولا يُقبل إلا حيث الطابور
> طويلٌ أصلًا. القيدُ كان يسوق الناس إلى أثقل اللجان، وهو نقيض ما وُضع له: قِيس الفارق بين
> أخفّ لجنة وأثقلها فوجد **٦٧٨ دقيقة** بدل **٩**. والحصّة تُقاس الآن بمقعدٍ مسموح، ولوفدٍ
> مقعدٌ واحد دائمًا.

## ما لم تكشفه قراءةُ الشيفرة

الشاشات كُتبت بأرقامٍ محسوبة ولم تُعايَن على لوحٍ حقيقي. فبُنيت أداةُ قياس تفتح متصفّحًا
على مقاسات تلفازٍ حقيقية، وتقيس الحجم المحسوب لكل نوع، وتحوّله إلى **المسافة التي يُقرأ
منها بالأمتار**، وتقيس **تباينه** بعد تركيب الطبقات الشفّافة، وتقول هل تتّسع الشبكة بلا
تمرير. تُشغَّل بـ `npm run qa:venue-legibility`.

وكشفت ثلاثة أشياء لم تكن لتُكتشف بالقراءة:

| ما كُشف | قبل | بعد |
|---|---|---|
| **تضارب اسم صنف.** `mizan-code` كان مأخوذًا لرقاقةٍ على سطحٍ فاتح تحمل `color:var(--emerald)` — أخضرَ داكنًا. فورثه كود القاعة على سطحٍ **داكن**. | تباين **١٫٣:١** — غير مرئيٍّ عمليًا | **١٣–١٥:١** باسمٍ خاص ولونٍ موروث |
| **الشبكة تُقزَّم.** الحدّ الأدنى ١٥٫٥rem أعطى ٧ أعمدة لا ٥، فضاقت الخليّة وضاق كودها. | يُقرأ من **٣٫٢ م** | **٥٫٦ م** وشبكة ٥×٢ |
| **تمريرٌ خفيّ.** حاوية التمرير تحمل حشوًا سفليًا لزرّ القفل، والطفل يطلب `min-h-screen` فوقه — فكل شاشة تُمرَّر ٨٨ بكسل ويسقط تذييلها تحت الحافة. | تذييل مقطوع | `min-h-full` ⇒ تتّسع بلا تمرير |

والكود المنادى به على تلفاز ٥٥ بوصة يُقرأ الآن من **١٩٫٢ م** — بعد أن كان ١٣٫٩.

> **تبقى معاينةٌ بالعين البشرية.** القياس يعطي أرقامًا صادقة عن الحجم والتباين والاتّساع،
> ولا يعطي حكمًا على الإيقاع ولا على ما تفعله الشاشة في قاعةٍ ممتلئة. نصف ساعةٍ أمام لوحٍ
> حقيقي تبقى الخطوة التي لا تُختصر.

## القواعد الذهبية

> **A display never decides and never writes. It renders a projection.**
> الشاشة لا تُقرّر ولا تكتب. تعرض إسقاطًا جاهزًا فقط.

> **No screen in the hall may reveal a question, its scope, or a score.**
> لا شاشة في القاعة تكشف سؤالًا ولا نطاقه ولا درجة. نطاق الانكشاف يُحسب، لا يُهدى.

> **Committee assignment must be explainable to the participant who received it.**
> إسناد اللجنة يجب أن يكون قابلًا للشرح لمن أُسند إليه — لا «هكذا اتفق».

> **Balance is measured in minutes, not in heads.**
> التوازن يُقاس بالدقائق لا بعدد الرؤوس. عشر لجان بمتوسطات مختلفة تجعل الطابور المتساوي
> عدديًا غير متساوٍ زمنيًا.

---

# الجزء الأول — اليوم (محقَّق من الكود)

## ١.١ الشاشات القائمة

| السطح | الملف | الحالة |
|---|---|---|
| شاشة الانتظار (قاعة) | `src/components/public/WaitingBoard.tsx` | منفَّذة |
| بوابة الحضور | `src/components/gate/KioskMode.tsx` | منفَّذة |
| خريطة تلاوة القاعة | `src/components/public/HallRecitationMap.tsx` | منفَّذة |
| وضع الحفل | `src/components/public/CeremonyView.tsx` | منفَّذة |
| **شاشة اللجنة** | — | **غير موجودة** |

`Committee Display` / «شاشة اللجنة» موجودة اليوم كـ**اسم دور جهاز فقط** في ثلاثة مواضع، بلا
أي مكوّن يعرضها:

- `src/types/index.ts:1001` — ضمن `DeviceRecord['role']`
- `src/components/admin/DeviceCenter.tsx:9` — ضمن قائمة الأدوار القابلة للإسناد
- `src/lib/ui-language.ts:286` — الترجمة `'Committee Display': 'شاشة اللجنة'`

أي أن النظام يسمح بإسناد جهاز لدور «شاشة اللجنة»، ثم لا يوجد ما يُعرض عليه.

## ١.٢ ما تعرضه شاشة الانتظار فعلًا

`WaitingBoard.tsx` يقرأ من المخزن مباشرةً (`participants`, `committees`, `competition`) ويعرض:

1. **«يُرجى التوجه»** — اللجان التي لديها `currentParticipantId`، في شبكة `sm:grid-cols-2`.
2. **«التالي»** — أول **٦** من طابور الانتظار **العام** للمسابقة كلها:
   `waiting.slice(0,6)` بعد الفرز بـ `queueOrderValue`.
3. تذييل: عدد المنتظرين، عدد اللجان العاملة، وسطر خصوصية.

## ١.٣ لماذا تنكسر عند عشر لجان

| المشكلة | السبب في الكود |
|---|---|
| بطاقات النداء تتجاوز الشاشة | `sm:grid-cols-2` × ١٠ لجان = ٥ صفوف من بطاقات بخط `text-5xl` على تلفاز واحد |
| «التالي» يصبح بلا معنى | `slice(0,6)` على طابور **عام**: المتسابق المُسند للجنة العاشرة قد لا يظهر أبدًا وإن كان التالي في لجنته |
| لا وقت متوقّع | الشاشة لا تعرض «كم يبقى» — والمتسابق لا يعرف هل يذهب للمصلى أم يبقى |
| لا مرجع زمني للصدق | لا يوجد «آخر تحديث» — شاشة متجمّدة تبدو حيّة |

## ١.٤ التوزيع اليوم — القرار الكامل

القرار نقيٌّ ومُختبَر في `src/lib/arrival-core.ts`، وأثره في `src/lib/store.ts:952` (`checkInParticipant`).

**وقت القرار:** لحظة مسح البطاقة عند البوابة. **لا يوجد توزيع قبل اليوم.**

**المرشّحات** (`compatibleCommitteesFor`, `store.ts:944`):

```
c.competitionId === participant.competitionId
&& c.status !== 'offline'
&& c.assignedCategories.includes(participant.categoryId)
&& !committeeHasHardConflict(c, participant)
```

**الاختيار** (`leastLoadedCommittee`, `arrival-core.ts:43`):

```
أقل عددًا من المنتظرين (status === 'in_queue')
ثم — عند التعادل — الأقل averageSessionMinutes
```

**ثبات الإسناد:** `participant.assignedCommitteeId || leastLoadedCommittee(...)` — لجنة أُسندت
مسبقًا لا تُبدَّل عند الوصول.

**الاحتياطي** (`store.ts:961`): حين تكون المجموعة المؤهَّلة فارغة، يُستخدم
`fallbackCommittees` = كل لجان المسابقة غير الـ `offline` بلا تعارض صلب — **بلا شرط الفئة**.

**آليات لاحقة موجودة:**

| الآلية | الملف | الدور |
|---|---|---|
| `chooseSessionCommittee` | `src/lib/session-start-core.ts` | يعيد التحقق عند بدء الجلسة؛ يفتح حادثة `conflict_routing` إن لم تُوجد لجنة آمنة |
| `recommendCommitteeElasticity` | `store.ts:3027` | يقترح أقل نقل يحسّن التوازن — **بموافقة بشرية**، لا تنفيذ تلقائي |
| `transferQueueParticipants` + `QueueJustice` | `store.ts:3020`, `operations/QueueJustice.tsx` | نقل مُدقَّق مع `PRESERVE_ORIGINAL_TURN` أو `MOVE_TO_END` |
| `multiRiwayahSmartRoute` | `src/lib/global-integrity-protocol.ts:36` | اختيار لجنة بأهلية الرواية + الصوت + AI — **لا يُستدعى من البوابة** |

## ١.٥ الملاحظات الحقيقية على التوزيع الحالي

1. **تعارض مقياس الحمل.** الوصول يعدّ الرؤوس (`arrival-core.ts:45`)؛ المرونة تحسب
   `count × averageSessionMinutes` (`store.ts:3029`). عند لجنة بـ ٦ دقائق وأخرى بـ ١٤، طابورٌ
   متساوٍ عددًا يعني ضعف الزمن — والبوابة لا ترى ذلك.

2. **الاحتياطي يخترق شرط الفئة.** `fallbackCommittees` لا يفلتر `assignedCategories`. النتيجة
   المحتملة: إسناد متسابق للجنة لا تحكم فئته، ثم اكتشاف ذلك عند بدء الجلسة. اختبار
   `tests/arrival-core.test.ts:131` يوثّق المبدأ الصحيح صراحةً — *«no panel is better than a
   wrong panel»* — لكن المخزن يناقضه بتمرير احتياطي أوسع.

3. **الجشع اللحظي.** القرار فردي وبلا نظرة للقادمين. وصول وفدٍ كامل في خمس دقائق يوزَّع
   بالتساوي عدديًا دون اعتبار لتركيبة الفئات أو الوفود.

4. **الرواية غير معتبرة عند البوابة.** `multiRiwayahSmartRoute` يفحص تأهيل لجنة التحكيم
   للرواية، ويُستدعى يدويًا من `IntegrityProtocolLab.tsx:49` فقط.

5. **التعارض الناعم مُهمَل.** `committeeHasHardConflict` يفلتر `hardConflict` وحده. الانتماء
   لنفس الوفد أو المؤسسة بلا إعلان صلب لا يُبعد ولو تفضيليًا.

6. **لا ضمان لتوازن الوفود.** لا قيد يمنع وقوع وفد كامل في لجنة واحدة. ومع وجود
   `committee-integrity.ts` الذي يقيس **تفاوت متوسط الخصم بين اللجان**، يصبح التوزيع نفسه
   عاملًا محتملًا في النتيجة لا مجرد ترتيب إداري.

7. **`averageSessionMinutes` ثابت.** يُضبط عند إنشاء اللجنة (`store.ts:2271`) من
   `questionDurationMinutes`، ولا يتعلم من الجلسات الفعلية.

## ١.٦ قيد المعمارية الحاكم: من يستطيع فتح شاشة أصلًا

هذا أهم قيد وأقلّه وضوحًا، ويجب حسمه قبل رسم أي شاشة.

**(أ) لا يوجد رابط للشاشات.** المسارات المتاحة في `src/App.tsx` هي `#register`, `#competition`,
`#journey`, `#guardian`, `#verify`, `#trust-verify`, `#reset-password`, `#manage-competition`.
شاشة الانتظار **ليست مسارًا**: تُفتح كطبقة فوق التطبيق عبر حالة `setWaitingBoard(true)` أو
حدث `mizan:open-venue` (`App.tsx:221`). أي أن تشغيل تلفاز يتطلب إنسانًا يسجّل الدخول ويفتح
الطبقة يدويًا، ولا يمكن استعادة الشاشة بعد إعادة تشغيل الجهاز.

**(ب) قواعد فايرستور تمنع الشاشة غير المصرَّح لها.** قراءة `participants` تشترط دورًا من
`['super_admin','org_admin','comp_admin','head_judge','judge','ops_manager','exception_host','delegation_manager','auditor']`
(`firestore.rules:136`)، و`committees` مثلها (`firestore.rules:177`). لا يوجد دور عرض،
و`broadcast_operator` نفسه غير مشمول.

**النتيجة العملية:** تشغيل عشر شاشات لجان اليوم يعني **عشرة أجهزة مسجَّلة بدور تشغيلي كامل،
معلّقة في قاعة بلا حارس**. هذا سطح خطر لا يُقبل، وهو وحده سبب كافٍ لبناء إسقاط عرض مستقل.

**(ج) المزامنة اللحظية متوفّرة.** `store.ts:806-836` يشترك بـ `onSnapshot` على وثيقة المسابقة
وعلى `participants` و`committees` وغيرها. أي أن حالة الطابور **تصل فعلًا** لكل جهاز مصرَّح
لحظيًا — البنية التحتية للشاشات المتعددة قائمة، وينقصها الإسقاط والصلاحية.

---

# الجزء الثاني — المقترح

## ٢.١ مبدأ العرض: مصدر واحد، عدسات متعددة

بدل أن تقرأ كل شاشة المخزن وتعيد اشتقاق منطقها (كما تفعل `WaitingBoard` اليوم)، يُبنى
**إسقاط واحد** تشتق منه كل الشاشات عدستها:

```
buildDisplayBoard(state) ──> DisplayBoard
                               ├── عدسة القاعة    → كل اللجان، مضغوطة
                               ├── عدسة اللجنة C7 → شريحة واحدة، مكبّرة
                               └── عدسة الجيب     → شريحة المتسابق نفسه
```

وحدة نقية في `src/lib/display-board.ts` بلا React وبلا مخزن، على نفس نمط `arrival-core.ts`
و`session-start-core.ts`: القرار نقيًّا ومُختبَرًا، والأثر في المخزن.

```ts
export interface CommitteeBoardSlice {
  committeeId: string;
  code: string;                 // C7
  venueHall: string;
  status: 'ready' | 'testing' | 'paused' | 'offline';
  nowCalling?: { code: string; sinceIso: string };
  next: { code: string; position: number }[];   // ٣ للجنة، ١ للقاعة
  waitingCount: number;
  estimatedWaitMinutes: number;
  completedCount: number;
}

export interface DisplayBoard {
  competitionId: string;
  competitionNameArabic: string;
  generatedAt: string;          // مرجع الصدق: الشاشة تعرضه
  committees: CommitteeBoardSlice[];
  totalWaiting: number;
  privacyMode: 'CODES_ONLY' | 'CODE_AND_FIRST_NAME';
}
```

**قواعد ملزِمة على الإسقاط:**
- لا يحمل معرّف متسابق، ولا اسمًا كاملًا، ولا درجة، ولا سؤالًا، ولا نطاقًا، ولا اسم محكّم.
- يحمل `generatedAt` دائمًا؛ الشاشة تعرض «آخر تحديث قبل ٩ ثوانٍ»، وتتحول لحالة **باهتة معلنة**
  إن تجاوز العمر ٦٠ ثانية. شاشة متجمّدة تقول إنها متجمّدة.

## ٢.٢ شاشة القاعة العامة — إعادة تصميم لعشر لجان

الطابور العام يُلغى ويحلّ محله **شبكة اللجان**:

```
┌──────────────────────────────────────────────────────┐
│  قاعة الانتظار · مسابقة …                     ١٤:٣٢  │
│                                      آخر تحديث: ٨ ث  │
├────────┬────────┬────────┬────────┬────────┬────────┤
│  C1    │  C2    │  C3    │  C4    │  C5    │        │
│ ▶ A-14 │ ▶ A-31 │  —     │ ▶ B-07 │ ▶ A-52 │        │
│ ⏭ A-19 │ ⏭ A-33 │ ⏭ C-02 │ ⏭ B-11 │ ⏭ A-55 │        │
│ ٤ · ٢٢د│ ٦ · ٣٥د│ ١ · ٦د │ ٣ · ١٨د│ ٥ · ٣٠د│        │
├────────┼────────┼────────┼────────┼────────┼────────┤
│  C6 …  │  C7 …  │  C8 …  │  C9 …  │  C10 … │        │
└────────┴────────┴────────┴────────┴────────┴────────┘
   الآن ▶   ·   التالي ⏭   ·   المنتظرون · الزمن المتوقع
```

- شبكة ثابتة `5×2` — كل اللجان في لقطة واحدة بلا تمرير.
- كل خلية: **الآن** + **التالي (واحد)** + عدد المنتظرين + الزمن المتوقع.
- **حركة النداء:** عند تغيّر `nowCalling` تومض الخلية ٣ ثوانٍ بلون دافئ ثم تهدأ. هذا يحل
  مشكلة «الشاشة تتغير ولا أحد ينتبه» دون صوت مزعج في قاعة تلاوة.
- `aria-live="polite"` على منطقة النداء لقارئ الشاشة، ونغمة نداء اختيارية عبر
  `/api/public/cue-audio` الموجود أصلًا (`server.ts:814`).

## ٢.٣ شاشة اللجنة — تصميم جديد

الفرق الجوهري: **شاشة القاعة تخدم كل الحاضرين؛ شاشة اللجنة تخدم من يقف أمام بابها.**
لذلك تعرض أقل، وأكبر، وتضيف ما لا يصلح للقاعة: التسلسل القريب والحالة التشغيلية.

```
┌──────────────────────────────────────────────────┐
│  اللجنة C7 · قاعة الطابق الثاني                  │
│                                                  │
│                    الآن                          │
│                                                  │
│                  A - 5 2                         │
│                  (خط ضخم)                        │
│                                                  │
│              ● تحت التلاوة · ٤:١٢                │
│                                                  │
│  ─────────────────────────────────────────────  │
│  التالي:   A-55   ·   B-08   ·   A-61            │
│  المنتظرون: ٥   ·   المتوقع: ٣٠ دقيقة            │
│                          آخر تحديث: ٦ ثوانٍ      │
└──────────────────────────────────────────────────┘
```

**ما تعرضه:** كود الحالي (ضخم)، الحالة (`جاهزة` / `تحت التلاوة` / `بين الأسئلة` / `قفل المحكمين` /
`متوقفة`)، مدة الجلسة الحالية، ثلاثة أكواد تالية، عدد المنتظرين، الزمن المتوقع، مرجع التحديث.

**ما لا تعرضه أبدًا — وهذه ليست تفضيلًا بل شرط نزاهة:**

| ممنوع | السبب |
|---|---|
| نص السؤال أو موضعه | من ينتظر خلف الباب يرى سؤال من قبله ⇒ يتسع نطاق الانكشاف. النظام يسجّل `exposure_radius` كدليل اعتراض (`global-integrity-protocol.ts:52`) — فلا يجوز أن تصنعه الشاشة |
| أي درجة أو خصم | الحكم لا يُعلن قبل ختمه؛ وإعلانه في الممر يضغط اللجنة والمتسابق التالي |
| أسماء المحكمين ونشاطهم | `committee-integrity.ts` يحظر ترتيب الأفراد صراحةً — والشاشة لا تلتف على الحظر |
| اسم المتسابق الكامل | افتراضيًا أكواد فقط، مع `privacyMode` تختاره الجهة |
| «سرعة اللجان» كترتيب | يحوّل التقدير الزمني إلى ضغط تشغيلي على التحكيم |

**حالة الخطأ الأهم:** إن انقطع التحديث، لا تُترك الشاشة تعرض كودًا قديمًا كأنه نداء حيّ.
تتحول إلى: «التحديث متوقف — آخر حالة معروفة قبل ٣ دقائق» بخلفية باهتة.

## ٢.٤ كيف تصل الشاشة إلى البيانات — الحل الموصى به

**إسقاط عرض عام واحد للمسابقة**، على نمط `public_competitions` القائم فعلًا
(`firestore.rules:62` — `allow read: if true`):

```
public_boards/{competitionId}     ←  DisplayBoard (أكواد فقط)
   يكتبه:  comp_admin | ops_manager  (كل ٥–١٠ ثوانٍ أو عند تغيّر)
   يقرأه:  الجميع بلا تسجيل دخول
```

ثم مسار في `App.tsx` قبل بوابة تسجيل الدخول:

```
#board?comp=<id>                 →  شاشة القاعة (كل اللجان)
#board?comp=<id>&panel=C7        →  شاشة اللجنة C7
#board?comp=<id>&panel=C7,C8&rotate=20   →  تناوب كل ٢٠ث حين تقلّ الشاشات عن اللجان
```

**لماذا هذا هو الحل الصحيح:**

| المكسب | التفصيل |
|---|---|
| صفر امتياز على الشاشة | لا حساب، لا جلسة، لا صلاحية كتابة. جهاز مسروق من القاعة لا يحمل شيئًا |
| لا يُسرّب أكثر مما يُنادى به صوتًا | المعروض أكواد ولجان — وهو عين ما ينادي به المنادي في القاعة |
| كتابة واحدة، شاشات بلا حدّ | ١٠ شاشات لجان + شاشة قاعة + هواتف المتسابقين = قارئو وثيقة واحدة |
| يعمل بعد إعادة التشغيل | الرابط يستعيد الشاشة تلقائيًا بلا إنسان |
| إطفاء فوري | حذف الوثيقة أو `published:false` يطفئ كل الشاشات دفعةً واحدة |
| تدهور شريف | آخر إسقاط يُخزَّن محليًا؛ الشاشة تعرضه معلنةً عمره بدل شاشة بيضاء |

**البديل المرفوض:** إسناد دور تشغيلي (`ops_manager`) لأجهزة العرض. يعطي الشاشة صلاحية قراءة
سجل المتسابقين كاملًا وصلاحيات كتابة، على جهاز غير مراقَب. لا يُقبل.

**الربط بسجل الأجهزة:** يبقى `DeviceRecord` بدور `Committee Display` + `committeeId` هو
**السجل الإداري** للشاشة (أين هي، متى آخر نشاط، هل تُسحب)، ويُولَّد رابطها من هناك مع QR
لتسهيل التوجيه — دون أن يمنح الرابط أي امتياز.

## ٢.٥ التوزيع المقترح — أربع طبقات

### الطبقة ١ — الأهلية (مرشّحات صلبة، لا تفاوض)

```
الفئة (assignedCategories)
∧ الرواية/القراءة مؤهَّلة في لجنة التحكيم        ← من multiRiwayahSmartRoute
∧ لا تعارض صلب معلن
∧ اللجنة ليست offline
∧ مسار الصوت جاهز إن اشترطته السياسة
```

**التغيير الحاسم:** إن كانت المجموعة فارغة ⇒ **لا إسناد + حادثة `conflict_routing`**، لا احتياطي
يخترق الفئة. هذا يطابق ما يوثّقه `tests/arrival-core.test.ts:131` أصلًا، ويصلح الملاحظة (٢).

### الطبقة ٢ — التوازن (بالدقائق لا بالرؤوس)

```
load(c) = Σ over waiting(c) [ avgSessionMinutes(c) ] + remainingMinutes(currentSession(c))
```

الأقل حملًا يفوز. فضّ التعادل **حتمي** ليبقى القرار قابلًا لإعادة الإنتاج في التدقيق:
أقل `averageSessionMinutes`، ثم أقل عددًا، ثم ترتيب `code` أبجديًا.

### الطبقة ٣ — قيود العدالة (تفضيلية، تُخفَّف عند الضرورة)

- **سقف الوفد:** لا يتجاوز وفدٌ واحد نسبة مضبوطة من طابور لجنة (٤٠٪ افتراضًا) ما دام بديل
  مؤهَّل موجودًا. يمنع وقوع وفد كامل تحت لجنة واحدة — وهو ما يجعل تفاوت اللجان الذي يقيسه
  `committee-integrity.ts` أثرًا على وفد بعينه.
- **مباعدة تركيبة الفئات** بين اللجان ما أمكن.
- تُخفَّف بالترتيب حين تُفرغ المجموعة، ويُسجَّل أيُّ قيد خُفِّف ولماذا.

### الطبقة ٤ — قرعة ملزِمة عند التعادل الحقيقي

عند تساوي مرشّحين بعد كل ما سبق، لا يُختار الأول عرضًا. تُجرى قرعة على نمط `FairDraw`
(`FAIRDRAW.md`): بذرة عشوائية + التزام `SHA-256` يضم المتسابق والمرشحين ونسخة السياسة.

فيصبح جواب «لماذا لجنتي هي C7؟»: *«لأن C3 و C7 تساوتا، وقرعة ملتزمة ببصمة `a7f3…` اختارت
C7، وتستطيع التحقق منها»* — بدل «هكذا وُزّع». **الإسناد يتحول من صدفة إلى إثبات.**

## ٢.٦ أنماط التوزيع الثلاثة — سياسة معلنة لا قرار مخبوء

تُرفع إلى `CompetitionPolicy` لتكون قابلة للنسخ والتجميد والتدقيق كبقية السياسات:

### `ON_ARRIVAL` — الإسناد لحظة المسح (السلوك الحالي، مُصلَحًا بالطبقات الأربع)
- **مناسب لـ:** الحضور المتدفق العشوائي، المسابقات المحلية.
- **عيبه الباقي:** جشع — قرار كل متسابق بمعزل عمن بعده.

### `PRE_ASSIGNED` — خطة مسبقة قبل اليوم
- تُبنى الخطة كاملة (لجنة + نافذة زمنية) وتُنشر في بطاقة المتسابق ورابط الرحلة
  (`JourneyAccess` موجود).
- **مناسب لـ:** الوفود والمسابقات الدولية — السفر والمرافقون والترجمة تحتاج معرفةً مسبقة.
- **عيبه:** تخلّف الحاضرين يُفرغ لجانًا ويُزدحم أخرى ⇒ يلزمه `QueueJustice` بكثافة.

### `WAVES` — الموجات (الأقوى لعشر لجان)

اليوم يُقسَّم موجات (ساعة مثلًا). كل موجة تُوزَّع **دفعةً واحدة** لا فردًا فردًا.

**لماذا الموجات تحديدًا عند عشر لجان؟** لأن خطأ الجشع يتراكم مع عدد اللجان والقيود. التوزيع
الفردي لا يملك ترف «التبديل»: ما إن يُسند المتسابق حتى يصبح تصحيحه نقلًا يمسّ الأسبقية.
الدفعة تسمح بالتبديل **قبل** أن يُصبح قرارًا.

والمتسابق يعرف **موجته** لا دقيقته: فلا يجلس أربع ساعات، ولا يفوته دوره.

**الخوارزمية:**

```
1) لكل (متسابق p، لجنة c):
     cost(p,c) = انتظار متوقع بالدقائق
               + غرامة تجاوز سقف الوفد
               + غرامة تكديس الفئة
               + ∞  إن سقطت الأهلية الصلبة
2) توزيع جشع أولي بالتكلفة الأدنى
3) دورتا تحسين بالتبديل: هل تبديل زوج (p1↔p2) يخفض أقصى انتظار دون كسر قيد صلب؟
4) المخرج: خطة + شرح لكل إسناد + بصمة القرار
```

`min-cost-flow` أو الخوارزمية المجرية تعطيان الأمثل، لكن **الجشع + تبديلان** يكفيان عمليًا
لعشر لجان ومئات المتسابقين، ويبقيان الكود مفهومًا وقابلًا للاختبار — وهو ما يخدم الثقة أكثر
من الأمثلية الرياضية.

**والخطة تبقى اقتراحًا حتى يعتمدها المسؤول**، على نفس نمط `recommendCommitteeElasticity`
القائم: النظام يقترح، والإنسان يوقّع.

## ٢.٧ الحلقة المغلقة — تعلُّم `averageSessionMinutes`

اليوم ثابت. المقترح: متوسط متحرك لآخر خمس جلسات مكتملة فعليًا في اللجنة، مع حدّ أدنى من
العينات قبل الاعتماد عليه (وإلا فالقيمة المُعدّة).

**أثره:** التقديرات الزمنية على الشاشات تصدق مع تقدم اليوم بدل أن تبقى نظرية.

**تحذير ملزِم:** يبقى هذا **وصفيًا لا حافزًا**. لا يُعرض «ترتيب سرعة اللجان» في أي سطح، ولا
يدخل أي تقييم. نفس فلسفة `committee-integrity.ts`: إشارة تشغيلية للأمانة العامة، لا ترتيب
لأحد. اللجنة التي تتأنى في حالة مشتبهة يجب ألا تُعاقب بمؤشر.

## ٢.٨ ما يراه المتسابق (نفس الإسقاط، عدسة ثالثة)

| اللحظة | ما يُعرض |
|---|---|
| قبل اليوم (`PRE_ASSIGNED`/`WAVES`) | الموجة أو النافذة الزمنية، ومكان اللجنة |
| عند الحضور | رقمه، لجنته، كم أمامه، الزمن المتوقع |
| عند اقتراب الدور | إشعار — `refreshQueueNotifications` قائم (`store.ts:909`) بـ `queue.next` و`queue.prepare` |
| عند نقل عادل | إشعار بالتغيير وسببه — الأسبقية محفوظة ما لم يُعلَن خلافها |

---

# الجزء الثالث — خطة التنفيذ

كل دفعة مستقلة وقابلة للدمج وحدها، مرتّبة بالأثر على الخطر.

| # | الدفعة | الملفات | الاختبارات |
|---|---|---|---|
| ١ | `display-board.ts` — الإسقاط النقي | جديد: `src/lib/display-board.ts` | `tests/display-board.test.ts`: شريحة لكل لجنة، حساب الزمن، سلوك القِدَم، خلوّ الإسقاط من الأسماء والأسئلة |
| ٢ | شاشة اللجنة + مسار العرض | جديد: `src/components/public/CommitteeDisplay.tsx`؛ تعديل: `App.tsx` | تشغيل يدوي + فحص Arabic UI audit |
| ٣ | إعادة تصميم شاشة القاعة لشبكة ١٠ | `WaitingBoard.tsx` | لقطة بعشر لجان بلا تمرير |
| ٤ | `public_boards` + الناشر + القواعد | `firestore.rules`, `store.ts`, `server.ts` | اختبار قواعد: قراءة مجهولة تنجح؛ كتابة مجهولة تفشل؛ الوثيقة خالية من الأسماء |
| ٥ | إصلاح التوزيع: الدقائق + إلغاء الاحتياطي المخترق + الرواية | `arrival-core.ts`, `store.ts` | توسيع `tests/arrival-core.test.ts`: اللجنة الأبطأ لا تفوز بتساوي العدد؛ لا إسناد خارج الفئة؛ حتمية فضّ التعادل |
| ٦ | الموجات + قيود العدالة + قرعة التعادل | جديد: `src/lib/distribution-plan.ts` | خطة لـ ١٠ لجان × ٢٠٠ متسابق: احترام القيود، تحسّن أقصى انتظار بعد التبديل، ثبات الناتج لنفس البذرة |

**الترتيب الموصى به للبدء:** ١ → ٢ → ٣. تعطي شاشات عاملة لعشر لجان **دون أن تمسّ منطق
التوزيع ولا العدالة** — أقل خطر وأعلى أثر مرئي. ثم ٤ لغلق ثغرة الامتياز. ثم ٥ و٦ وهما
تغييران سلوكيان يحتاجان قرارًا معلنًا ووثيقة سياسة.

---

# الجزء الرابع — قرارات تحتاج توقيع الجهة

١. **الخصوصية على الشاشة:** أكواد فقط (الافتراض)، أم كود + اسم أول؟ بعض المسابقات تريد الاسم
   ليُنادى به صوتًا.
٢. **هل شاشة اللجنة تعرض التالي؟** عرضه يقلّل الفوضى ويزيد الاستعداد، لكنه يخبر المتسابق
   أن دوره بعد دقائق — وقد يزيد التوتر قبل التلاوة.
٣. **مصير الاحتياطي المخترق للفئة:** يُلغى (توصيتي) أم يبقى مع تنبيه صريح للمسؤول؟
٤. **من يعتمد خطة الموجة؟** `comp_admin` وحده، أم `ops_manager` أيضًا؟
٥. **نمط التوزيع الافتراضي للجهة:** `ON_ARRIVAL` أم `WAVES`؟
٦. **سقف الوفد في اللجنة الواحدة:** ٤٠٪ رقم مقترح لا مُثبت — يحتاج قرار الجهة.

---

# الجزء الخامس — ما لا أوصي به

| المضاد | السبب |
|---|---|
| عرض السؤال أو موضعه على أي شاشة | يوسّع نطاق الانكشاف ويهدم أثر `FairDraw` |
| عرض الدرجات لحظيًا في القاعة | يضغط اللجنة والمتسابق التالي، ويسبق ختم النتيجة |
| ترتيب اللجان علنًا بالسرعة أو الصرامة | يحوّل مؤشرًا تشغيليًا إلى حافز يفسد التحكيم |
| شاشة تكتب أي حالة | الشاشة سطح غير مراقَب؛ الكتابة منها سطح هجوم بلا مقابل |
| إعادة توزيع تلقائية بلا موافقة | يمسّ الأسبقية — وهي أكثر ما يُشتكى منه في القاعات. النظام يقترح، والإنسان يوقّع |
| إسناد دور تشغيلي لأجهزة العرض | يمنح صلاحية قراءة السجل الكامل لجهاز معلّق في ممر |
| شاشة تعرض بيانات قديمة كأنها حيّة | تفقد الجمهور ثقته في كل شاشة بعدها. القِدَم يُعلَن دائمًا |
