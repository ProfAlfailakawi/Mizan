# MIZAN — حزمة تنفيذ جميع الملاحظات

**الأساس المقصود:** `ProfAlfailakawi/Mizan` عند commit:

`b8e4e1918202a7bc73ac7a5b4690a27c50a0af83`

هذه **ليست نسخة كاملة من المشروع**. الحزمة تحتوي فقط على الملفات التي تغيرت بالكامل، واختبارات التعديل، وسكربت آمن لتعديل الملفات الكبيرة في مكانها. لا يوجد مجلد wrapper داخل ZIP.

## طريقة التطبيق

1. تأكد أن المشروع الذي أمامك مبني على commit `b8e4e1` المذكور أعلاه.
2. فك محتويات ZIP **مباشرة في جذر المشروع** بحيث ترى `src/` و`server/` و`tests/` في أماكنها الأصلية.
3. من جذر المشروع شغّل:

```bash
python3 APPLY_ALL_MIZAN_NOTES.py
```

السكربت يجهز كل تعديلات الملفات الكبيرة في الذاكرة أولًا، ويتحقق من anchors الخاصة بالنسخة المطلوبة، ثم يكتبها دفعة واحدة. إذا وجد أن النسخة لا تطابق الأساس، يتوقف بدل أن يترك تعديلات كبيرة نصف مكتملة.

4. بعدها شغّل التحقق الكامل للمشروع:

```bash
npm run check
```

## ما تم تنفيذه

### 1) الطوارئ + «اشرح لي هذه الواجهة» + البحث السريع

أصل المشكلة كان أن overlays ذات `position: fixed` تُرسم داخل الـHeader الـsticky الذي يستخدم `backdrop-filter`، فينشأ containing/stacking context يجعل النافذة تظهر كشريط مقصوص أعلى الشاشة.

تم علاج السبب من الجذر:

- `Modal` أصبح يُرسم عبر React Portal إلى `document.body`.
- `ClarityGuide` أصبح Portal إلى `document.body`.
- `CommandPalette` أصبح Portal إلى `document.body`.
- نافذة الطوارئ تستعمل `Modal` أصلًا، ولذلك تستفيد تلقائيًا من العلاج.
- تم رفع مستوى الـoverlay فوق الهيدر مع الحفاظ على التركيز، Escape، وARIA.

النتيجة: النوافذ والأدراج تظهر داخل viewport كامل بدل أن تُحصر في ارتفاع الهيدر.

### 2) اسم النطاق لا يُقص

تم إزالة سلوك الاختصار البصري من نقاط عرض نطاق الجهة، وإضافة wrapping مناسب لـLTR domains:

- `TenantConsole`
- قائمة الجهات داخل `RolePortals`

النطاق الآن يظهر كاملًا، ويلتف عند الضرورة بدل `...dr-alfailakawi.com`.

### 3) إلغاء دور «المسؤول العلمي»

الدور `scientific_admin` أصبح **Retired / legacy-readable only** حتى لا تتعطل سجلات قديمة موجودة على القرص، لكنه لم يعد دورًا صالحًا للاستخدام الجديد.

تمت إزالته من:

- RoleSwitcher
- ExperienceHub
- ClarityGuide
- شاشة دعوة المستخدمين
- App role routing
- الصلاحيات النشطة
- Grant matrices
- هوية الدخول المقبولة
- server endpoint role allowlists
- اختيار مراجع الاعتراضات في Competition Overview

السجلات القديمة التي تحمل هذا الدور لا تُحوّل تلقائيًا إلى دور أقوى؛ يجب إعادة إسناد صاحب الحساب إلى دور معتمد. هذا fail-closed ويمنع ترقية صلاحية صامتة.

الوظائف العلمية نفسها لم تُحذف من ميزان؛ انتقلت السلطات الخادمية التي كانت محصورة بهذا الدور إلى الجهات المناسبة مثل مالك المنصة / مدير الجهة / رئيس التحكيم حسب السياق. المطلوب كان حذف **الدور**، لا حذف سلامة المصدر القرآني.

### 4) «مسار المسابقة» صار مفهومًا بكلمة لكل مرحلة

الأرقام بقيت كما هي للحفاظ على جمال المسار، وتحت كل رقم يظهر الاسم الحقيقي للمرحلة من `workflowLabel`، مثل التسجيل، التحقق، التحكيم، الختم… حسب الـworkflow الفعلي للمسابقة.

لا توجد أسماء وهمية أو مسار ثابت؛ الكلمات تأتي من المراحل المفعلة فعلًا.

### 5) لا قوالب أو بيانات مستخدم افتراضية

تم تنظيف نقاط الـpreset التي كانت توحي أن المستخدم اختار شيئًا مسبقًا:

- حذف شريط قوالب البداية الثلاثة من «بصمة/هوية المسابقة».
- إيقاف تعيين `international-hifz` تلقائيًا عند إنشاء مسابقة جديدة.
- المسابقة الجديدة تبدأ بفئات فارغة بدل نسخ فئات مسابقة أخرى.
- لا `New Edition` افتراضي؛ الإصدار يبدأ فارغًا.
- حذف الشعارات التجريبية الثلاثة من Tenant Brand Studio.
- حذف أسماء جهة وهمية مثل `Organization` / `الجهة المنظمة` عند الحفظ.
- اختيار الدور في شاشة إضافة مستخدم يبدأ فارغًا؛ المستخدم يختار الدور بنفسه.
- تم استبدال أرقام إحصائية وهمية مثل «12 مستخدمًا مفوضًا» بعدّ حقيقي من الحالة.

بقيت فقط defaults التقنية/الأمنية اللازمة لعمل النظام نفسه؛ لا تُعرض على أنها بيانات اختارها العميل.

### 6) رمز التفعيل أصبح QR بدل الرمز الطويل

بعد إنشاء المستخدم:

- لا يظهر الرمز الخام الطويل ككتلة نصية.
- يظهر QR واضح ونظيف «مرة واحدة».
- في النطاقات المعتادة يحمل QR رابطًا من الشكل `/#a=<token>`.
- عند مسحه بكاميرا الهاتف يفتح ميزان.
- بعد تسجيل دخول الشخص بالبريد المدعو، إذا كان الحساب غير مفعّل، يربط ميزان الدعوة تلقائيًا.
- التفعيل ما زال one-time لأن الخادم يحتفظ فقط ببصمة token ويحذفه بعد الربط.
- يوجد Copy fallback صغير للحالات التشغيلية فقط، وليس الرمز الطويل كواجهة أساسية.

بالنسبة للنطاق الرسمي الحالي، رابط QR مع token يقع ضمن سعة QR المختصرة المستخدمة في المشروع.

### 7) عزل المستخدمين بين الجهات

الواجهة ليست الحارس الوحيد. العزل أصبح في Repository/Server:

- مدير الجهة يرى مستخدمي جهته فقط.
- مدير المسابقة يبقى داخل نطاق المسابقة المفوضة له.
- المستخدم الذي أُضيف لجهة لا يظهر في جهة أخرى.
- Super Admin يمكنه اختيار جهة وإدارتها بصفته مالك المنصة، لكن tenant admin لا يستطيع تمرير `organizationId` آخر لتجاوز العزل.

### 8) حماية Super Admin من الجهة

حتى لو وُجد سجل Super Admin في نفس نطاق بيانات قديم:

- لا يظهر لمدير الجهة ضمن قائمة مستخدمي الجهة.
- لا يمكن لمدير الجهة تعليقه.
- لا يمكن لمدير الجهة حذفه.
- الحماية enforced في backend وليست إخفاء زر فقط.

### 9) حذف مستخدم

تمت إضافة «حذف المستخدم» مع تأكيد بصري هادئ.

تقنيًا هو audit-preserving removal:

- الحساب يتحول إلى `REVOKED`.
- الصلاحيات النشطة/المعلقة تتحول إلى `REVOKED`.
- الجلسات النشطة تُغلق.
- المستخدم يختفي من قائمة المستخدمين الفاعلين فورًا.
- لا تُمسح الأدلة التاريخية أو سجل التدقيق.

هذا يحقق حذف الوصول من الجهة دون تدمير سجل نزاهة مسابقة رسمية.

### 10) تفويض مدير الجهة بدون «شخص ثانٍ» لكل حساب

تم اعتماد التسلسل المطلوب:

`Super Admin → يعتمد مدير الجهة → مدير الجهة يدير فريقه`

لذلك:

- مدير الجهة يضيف مدير المسابقة، المحكم، رئيس التحكيم، التشغيل… ضمن Grant Matrix مباشرة.
- مدير المسابقة يضيف الأدوار المسموحة له داخل مسابقته مباشرة.
- الدعوة الجديدة تصبح `READY` مباشرة وتحصل على QR التفعيل.
- لا توجد موافقة شخص ثانٍ لمجرد إنشاء موظف.
- دعوات `PENDING_APPROVAL` القديمة فقط يمكن ترقيتها إلى النظام الجديد لأغراض migration.

**لم يتم إلغاء فصل الصلاحيات من القرارات الحرجة.** مثل full retest الاستثنائي ما زال يحتاج سلطة مستقلة مختلفة، وكذلك آليات quorum/sealing تبقى كما هي.

### 11) MFA لا يعطل المحكم ومدير المسابقة افتراضيًا

تم فصل سياسة حماية مالك المنصة عن سياسة فريق الجهة:

- `super_admin`: MFA مطلوب افتراضيًا، ويمكن التحكم به عبر `MIZAN_REQUIRE_MFA_FOR_SUPER_ADMIN` / `VITE_REQUIRE_MFA_FOR_SUPER_ADMIN`.
- موظفو الجهة مثل `org_admin`, `comp_admin`, `head_judge`, `judge`, `auditor`: MFA **اختياري افتراضيًا** ولا يصبح إلزاميًا إلا إذا كانت `MIZAN_REQUIRE_MFA_FOR_SENSITIVE=true` و`VITE_REQUIRE_MFA_FOR_SENSITIVE=true`.

وبذلك لا يدخل المحكم أو مدير المسابقة في طريق مسدود بسبب عامل ثانٍ لم تقرر الجهة أصلًا فرضه.

## الملفات الكاملة الموجودة في ZIP

- `server/identity-governance.ts`
- `src/lib/useMizanAuth.ts`
- `src/lib/operational-integrity.ts`
- `src/lib/permissions.ts`
- `src/components/design-system/Modal.tsx`
- `src/components/design-system/CommandPalette.tsx`
- `src/components/design-system/ClarityGuide.tsx`
- `src/components/design-system/RoleSwitcher.tsx`
- `src/components/admin/IdentityGovernance.tsx`
- `tests/operational-continuity-identity.test.ts`
- `tests/user-notes-2026-09-08.test.ts`

## الملفات الكبيرة التي يعدلها السكربت في مكانها

- `server.ts`
- `src/App.tsx`
- `src/lib/store.ts`
- `src/lib/ui-language.ts`
- `src/components/admin/CompetitionOverview.tsx`
- `src/components/admin/TenantBrandStudio.tsx`
- `src/components/admin/TenantConsole.tsx`
- `src/components/admin/RolePortals.tsx`
- `src/components/public/ExperienceHub.tsx`

## التحقق الذي أُجري على الحزمة هنا

- Python patcher syntax: PASS.
- Patcher transactional smoke test against the exact baseline anchors used in the patch: PASS.
- Patcher idempotency smoke test: PASS.
- `server/identity-governance.ts` TypeScript was emitted locally; السلوك الفعلي لتفويض الجهة/العزل/التفعيل المباشر/الحذف/الدور الملغى تم تشغيله كـNode smoke tests: PASS.
- Static checks للـPortal/QR/no-presets/domain wrapping/delete API/MFA policy موجودة في test suite المرفق.

لم يتم تشغيل `npm run check` الكامل هنا لأن الحزمة المقصودة ليست المشروع كاملًا ولا تحتوي `node_modules`. لذلك لا يُدّعى نجاح build كامل قبل وضعها فوق checkout الحقيقي وتشغيل الأمر أعلاه.

## النشر إلى GitHub

هذه الحزمة **لا تدفع ولا تنشر** شيئًا. تكامل GitHub في هذه المحادثة كان يسمح بالقراءة لكنه أعاد `403 Resource not accessible by integration` عند الكتابة. بعد اعتماد الحزمة وتشغيل `npm run check` في المشروع الحقيقي يمكن حل صلاحية الدفع كخطوة منفصلة.
