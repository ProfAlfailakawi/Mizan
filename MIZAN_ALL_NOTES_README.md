# MIZAN — حزمة إصلاح الخطأ الحالي وتنفيذ جميع ملاحظات 2026-09-08

## الجذر الصحيح
هذه **Patch ZIP فقط** وليست المشروع كاملًا. لا يوجد أي مجلد تغليف داخل ZIP.

فك محتويات ZIP **مباشرة في جذر مشروع MIZAN**؛ بعد الفك يجب أن ترى مباشرةً:

- `APPLY_ALL_MIZAN_NOTES.py`
- `APPLY_AND_CHECK.sh`
- `src/`
- `server/`
- `tests/`

## النسخ المدعومة
- الأساس الأصلي: `b8e4e1918202a7bc73ac7a5b4690a27c50a0af83`
- الـcommit غير المكتمل: `b2f5ed09d59058fe834dfac93f61abb2e603a80a`
- الـhead الحالي الذي فشل في CI: `736f0bebaad0442e156f118d4a67bdbc14936280`

## الخطأ الحالي الذي عولج
GitHub Actions run رقم 167 على commit `736f0be` وصل إلى **465 اختبارًا**:

- نجح: **464**
- فشل: **1**

الفشل الوحيد كان في:

`large-file notes were applied: labels, no presets, full domains, delete API and staff MFA opt-in`

والسبب المباشر أن `CompetitionOverview.tsx` ما زال بدون `w-14 text-center`؛ أي أن ملفات التغيير الصغيرة والاختبارات دخلت إلى المستودع، لكن سكربت تعديل الملفات الكبيرة لم يُشغّل قبل الـcommit.

هذه الحزمة تعالج السبب نفسه: سكربت `APPLY_ALL_MIZAN_NOTES.py` يطبّق جميع تعديلات الملفات الكبيرة بطريقة transaction-safe ثم يرفض أي تطبيق جزئي إذا لم تطابق الـanchors المتوقعة.

## التنفيذ
من جذر المشروع شغّل أمرًا واحدًا:

```bash
bash APPLY_AND_CHECK.sh
```

وهو ينفذ:

```bash
python3 APPLY_ALL_MIZAN_NOTES.py
npm run check
```

لا تعمل commit أو push قبل أن ترى:

```text
MIZAN notes patch applied successfully.
```

ثم يمر `npm run check` كاملًا.

## ما تعالجه الحزمة
- إصلاح قص نوافذ الطوارئ و«اشرح لي هذه الواجهة» والبحث عبر Portal حقيقي إلى `document.body`.
- إظهار نطاق الجهة كاملًا بدل الاختصار بـ `...`.
- إلغاء دور `scientific_admin` من الاستخدام التنفيذي والجديد مع إبقاء قراءة آمنة للسجلات القديمة فقط.
- إضافة اسم المرحلة الحقيقي تحت كل رقم في «مسار المسابقة».
- إزالة قوالب/هوية/شعارات وبيانات افتراضية توحي أن المستخدم اختارها مسبقًا.
- جعل المسابقة الجديدة تبدأ فارغة بلا template أو فئات منسوخة.
- QR تفعيل لمرة واحدة بدل عرض الرمز الطويل كواجهة أساسية.
- عزل مستخدمي كل جهة على الخادم.
- منع مدير الجهة من إدارة أو حذف Super Admin.
- إضافة حذف مستخدم كإلغاء وصول يحفظ Audit Trail.
- تفويض Super Admin → Org Admin → فريق الجهة/المسابقة ضمن النطاق الصحيح.
- جعل MFA للطاقم اختيارية بسياسة صريحة، مع بقاء حماية المالك منفصلة.
- إزالة الأرقام التجريبية واستبدالها بأرقام فعلية.
- تصحيح أهداف الأزرار التفاعلية إلى 44×44 على الأقل.

## مهم
- الحزمة **لا تحتوي المشروع كاملًا**.
- الحزمة **لا تعمل push** إلى GitHub.
- الحزمة **لا تنشر** إلى Cloud Run.
- المسارات داخل ZIP هي نفس مسارات المستودع من الجذر مباشرةً.
