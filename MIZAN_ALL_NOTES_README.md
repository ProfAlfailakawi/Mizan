# MIZAN — الحزمة المصححة لجميع ملاحظات 2026-09-08

## الأساس
- الأساس الأصلي: `b8e4e1918202a7bc73ac7a5b4690a27c50a0af83`
- الحزمة تعمل كذلك فوق الـcommit غير المكتمل الذي ظهر في CI: `b2f5ed09d59058fe834dfac93f61abb2e603a80a`
- لا تحتوي المشروع كاملًا، ولا يوجد wrapper folder.

## سبب فشل CI الذي أرسلته
قراءة Job `101987788804` أظهرت 465 اختبارًا: 461 نجح و4 فشل.

1. `interaction-accessibility.test.ts`
   - ثلاثة أزرار أيقونية في `IdentityGovernance.tsx` كانت 40×40 (`w-10 h-10`).
   - تم رفعها إلى 44×44 (`w-11 h-11`).

2. `owner-invite-self-serve.test.ts`
   - الاختبار كان يثبت السياسة القديمة: مدير الجهة يحتاج شخصًا ثانيًا.
   - تم تحديثه لسياسة التفويض المعتمدة: المالك يعتمد مدير الجهة، ومدير الجهة يضيف فريقه مباشرة داخل نطاقه.

3. `round30-launch-readiness.test.ts`
   - الاختبار كان يتوقع 14 دورًا بعد إلغاء `scientific_admin`.
   - تم تحديثه إلى 13 دورًا نشطًا، مع اختبار صريح أن الدور الملغى لا يدخل جلسة تشغيلية.

4. `user-notes-2026-09-08.test.ts`
   - السبب لم يكن أن منطق الاختبار خطأ؛ السبب أن الملفات الكبيرة لم تُعدّل أصلًا.
   - مقارنة Git بين `b8e4e1` و`b2f5ed0` أثبتت أن `APPLY_ALL_MIZAN_NOTES.py` أُضيف إلى الريبو، لكن لم يُشغّل قبل الـcommit؛ لذلك `CompetitionOverview.tsx`, `TenantBrandStudio.tsx`, `TenantConsole.tsx`, `RolePortals.tsx`, `App.tsx`, `server.ts`, `store.ts` وغيرها بقيت كما كانت.
   - الحزمة المصححة تبقي التعديل transaction-safe، لكن يجب تشغيل السكربت قبل الـcommit.

## طريقة التطبيق الصحيحة
فك ZIP في **جذر مشروع MIZAN الحالي** بحيث تظهر `src/` و`server/` و`tests/` مباشرة.

ثم نفّذ:

```bash
python3 APPLY_ALL_MIZAN_NOTES.py
npm run check
```

أو أمر واحد:

```bash
bash APPLY_AND_CHECK.sh
```

**لا تعمل commit قبل أن يظهر:**

```text
MIZAN notes patch applied successfully.
```

ثم يجب أن يمر `npm run check`.

## ما تعالجه الحزمة كاملة
- Portal حقيقي للطوارئ والدليل السريع والبحث حتى لا تنقص النوافذ تحت الـsticky header.
- النطاقات الطويلة تظهر كاملة بلا `...`.
- إلغاء `scientific_admin` من الأدوار التنفيذية والواجهات الجديدة، مع توافق قراءة آمن للسجلات القديمة فقط.
- مسار المسابقة يعرض كلمة المرحلة تحت كل رقم.
- إزالة قوالب بصمة المسابقة الجاهزة.
- المسابقة الجديدة تبدأ فارغة بلا template أو فئات/هوية/بيانات منسوخة.
- إزالة شعارات الهوية التجريبية والأسماء الوهمية.
- لا اختيار دور افتراضي عند دعوة مستخدم.
- QR تفعيل لمرة واحدة بدل عرض الرمز الطويل.
- عزل مستخدمي الجهات من الخادم.
- حماية Super Admin من الظهور/التعليق/الحذف بواسطة مدير جهة.
- حذف مستخدم كإلغاء وصول محفوظ في Audit Trail.
- تفويض الثقة: Super Admin -> Org Admin -> فريق الجهة/المسابقة مباشرة.
- لا موافقة شخص ثانٍ لإنشاء المحكم أو مدير المسابقة.
- استمرار فصل الصلاحيات في القرارات الحرجة مثل Full Retest الاستثنائي.
- MFA للـSuper Admin افتراضيًا، وMFA للطاقم اختياري بسياسة صريحة للجهة.
- إزالة الأرقام التجريبية مثل «12 مستخدمًا مفوضًا» واستخدام أرقام فعلية.
- تصحيح أهداف الأزرار التفاعلية إلى الحد الأدنى 44×44.

## ملاحظة عن الدفع
هذه الحزمة لا تدفع إلى GitHub ولا تنشر Cloud Run. بعد نجاح `npm run check` يمكن عمل commit/push بالطريقة التي تعتمدها.
