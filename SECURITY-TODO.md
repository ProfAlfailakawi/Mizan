# SECURITY-TODO — إصلاحات مؤجّلة تحتاج موافقة المالك

هذه القائمة تسجّل مخاطر ومسائل **لم تُطبَّق** في جولة التنظيف الآمن هذه لأنها إمّا قرار منتج/محتوى حسّاس أو قد تكسر التشغيل أو تحتاج مراجعة مختص. لم يُغيَّر أي منطق تشغيل.

## 0. الأخطر — البناء الحالي يُسقط تطبيق React بالكامل (قرار منتج للمالك)
- الـcommit الأخير `7cdb968` استبدل `index.html` الجذري (كان يحمّل `/src/main.tsx`) بصفحة العرض المستقل (الديمو) التي تحمّل `app.js`.
- النتيجة: أمر البناء `vite build` صار ينتج **الديمو فقط**، ويُسقط تطبيق React كاملاً (كل ما تحت `src/`، ~1.35MB: `App.tsx`, `main.tsx`, `components/`, `lib/store.ts`، إلخ).
- `src/main.tsx` لا يزال موجوداً وسليماً ويستورد `App.tsx` و`ArabicInterfaceGuard`، لكن لا نقطة دخول HTML تشير إليه الآن.
- **لم يُتَّخذ أي إجراء ولم يُحذف أي ملف تحت `src/`** — يبدو التطبيق كله «يتيماً» بسبب هذا التغيير فقط، وحذفه سيكون كارثياً.
- **قرار منتج للمالك:** إن كان التطبيق (React) هو المقصود للنشر، يجب إعادة `index.html` ليحمّل `/src/main.tsx`، أو فصل الديمو في مسار مستقل (مثل `demo.html` أو مجلد `/demo`). قد يكون المالك تعمّد نشر الديمو — لذلك تُرك القرار له.

## 1. مفاتيح Google API في تاريخ git — تدوير + تنظيف تاريخ
- `SECURITY.md` يعترف بأن مفتاح Google API كان مكشوفاً وأُزيل من المصدر/الإعدادات (قسم «2026-09-02 security hardening»).
- حذف المفتاح من الشجرة أو من ZIP **ليس** إبطالاً. المطلوب: **تدوير/إبطال المفتاحين عند المزوّد فوراً**، ثم **تنظيف تاريخ git** (يتطلب إعادة كتابة تاريخ + force-push بموافقة المالك، خارج نطاق هذه الجولة التي تضيف commits جديدة فقط).

## 2. رموز الوقف (WAQF_GLYPH) في app.js — محتوى ديني حسّاس، لا يُعدَّل بلا مختص
- الخريطة الحالية في `app.js:57`:
  `const WAQF_GLYPH = { tam:'ۘ'(U+06D8), kafi:'ۖ'(U+06D6), hasan:'ۖ'(U+06D6), lazim:'ۘ'(U+06D8) };`
- التحقق الفعلي (قبل أي تعديل) أظهر أن `lazim` يستخدم بالفعل **U+06D8** ولا وجود لـ **U+0658** في الملف — أي أن الوصف الأصلي للعطل («U+0658 و U+06D6 بدل U+06D8 للوقف اللازم») لا يطابق المحتوى الحالي حرفياً.
- تبقى المسألة قائمة كنقطة مراجعة: الخريطة تجمع `hasan` و`kafi` على رمز واحد (U+06D6)، وصحّة الرموز لكل نوع وقف تحتاج **مراجعة مختص في رسم المصحف** قبل أي تعديل.
- **لم يُطبَّق أي إصلاح** — قرار محتوى ديني حسّاس.

## 3. الختم (Seal) والنصاب (Quorum) يُحسبان في المتصفح بلا مسار خادمي
- في `src/lib/store.ts` (دالة `sealResults` قرابة السطر 800) يجري ختم النتائج (SHA-256 عبر Web Crypto) والتحقق من نصاب الموافقة المزدوجة على العميل (المتصفح).
- لا يوجد مسار تحقق خادمي مستقل يُصادق الختم/النصاب. مستخدم متحكّم في العميل يمكنه تجاوز المنطق. يحتاج مفاتيح توقيع خادمية/HSM ومستودع تدقيق ملحق-فقط (append-only) كما يلمّح `SECURITY.md` نفسه.

## 4. FairDraw بلا فصل زمني بين الالتزام والكشف
- آلية القرعة العادلة (`src/lib/fairdraw.ts` / `FAIRDRAW.md`) تعتمد commit-reveal لكن بلا فصل زمني مفروض بين مرحلة الالتزام (commit) ومرحلة الكشف (reveal).
- بلا هذا الفصل يضعف ضمان عدم التلاعب. يحتاج مراجعة تصميمية: فرض نافذة زمنية/بلوك بين المرحلتين وتخزين الالتزامات خادمياً قبل السماح بالكشف.

## 5. مصفوفة الإنجاز تدّعي 102 بند بلا بناء/اختبار منفَّذ
- `docs/changelog/MIZAN_COMPLETION_MATRIX.md` (نُقل في هذه الجولة) يدّعي اكتمال 102 بند.
- لم يُنفَّذ `npm run build` ولا `npm run test` للتحقق من هذه الادعاءات في هذه الجولة. الادعاءات غير مُتحقَّق منها ويجب عدم الاعتماد عليها كدليل جاهزية.

---
## ملاحظات جولة التنظيف (منفَّذة، للتوثيق فقط)
- **تبعيات ميتة لم تُحذف من `package.json`** (بحسب التعليمات): `@google/genai` (يظهر فقط في تصريح وحدة داخل `stubs.d.ts`، بلا أي استيراد فعلي) و`motion` (بلا أي استيراد في الشجرة). تُركت في `package.json`.
- **`server/quran-alignment-engine.ts` لم يُحذف**: رغم وجود `server/alignment/engine.ts` الأكبر، فإن `quran-alignment-engine.ts` **مستورَد فعلاً** من `server/quran-intelligence-service.ts` و`tests/quran-intelligence.test.ts` — فهو ليس تكراراً ميتاً، وحذفه يكسر التشغيل والاختبار.
- لا توجد مخرجات بناء ملتزمة في git (`dist/`/`build/` مُغطّاة أصلاً في `.gitignore`)؛ أُضيفت أسطر أمان لـ`server.cjs` و`*.tsbuildinfo`.
