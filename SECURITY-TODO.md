# SECURITY-TODO — بنود معمارية مفتوحة تحتاج قرار المالك

هذه القائمة تسجّل مخاطر **مُتحقَّق منها في الشيفرة** ولم تُطبَّق لها معالجة، لأن كلاً منها
تغيير معماري يمسّ حدود الثقة (خادم/عميل) ولا يصحّ إنجازه كتعديل تجميلي داخل المتصفح.
لم يُغيَّر أي منطق تشغيل عند كتابة هذا الملف.

الموضع من التوثيق: `SECURITY.md` يذكر البندين إشارةً؛ هذا الملف يثبّت **الدليل من الشيفرة**
و**شكل الحلّ المطلوب** حتى لا يُعاد اكتشافهما في كل مراجعة.

---

## 1. الختم والنصاب يُحسبان في المتصفح بلا مسار خادمي — خطورة: عالية

### الدليل
- `src/lib/store.ts` — `sealResults()` (نحو السطر 800): يُبنى `payload` من `globalState.results`
  ثم `const checksum = await sha256(payload)` ويُخزَّن في `sealMetadata.cryptographicChecksum`
  بصيغة `SHA256:<hex>`. هذه **بصمة (hash) لا توقيع (signature)**: لا مفتاح فيها، فأي طرف
  يمكنه إعادة إنتاجها لأي نتائج يختارها.
- النصاب كذلك على العميل: `ensureQuorumAction` / `approveQuorumAction` / `executeQuorumAction`
  (`src/lib/store.ts` نحو 1555‑1558) تعمل كلها على `globalState.quorumActions` في الذاكرة.
- الحالة كلها تُحفظ في `localStorage` (`store.ts:283` — `localStorage.setItem(STORAGE_KEY, ...)`)،
  و`store.ts:64` يقرأها عند الإقلاع. أي شخص يفتح أدوات المطوّر يستطيع تحرير الموافقات
  وحالة `executed` مباشرة.
- **لا يوجد أي مسار خادمي مقابل**: مسح `server.ts` لكل نقاط `/api/...` لا يُظهر أي نقطة
  لختم النتائج أو لاعتماد النصاب. الموجود `POST /api/enterprise/trust/sign` و`POST /api/trust/verify`
  و`POST /api/audit/events` فقط — أي أن قدرة التوقيع الخادمي **موجودة لكن مسار الختم لا يستدعيها**
  (بحث `trust/sign` في `src/` لا يعطي أي استدعاء؛ الظهور الوحيد هو راية القدرة
  `trustSigningConfigured` في `src/lib/runtime-capabilities.ts`).
- الشيفرة نفسها تعترف بذلك: `store.ts:1208` يضيف عائق
  `SERVER_QUESTION_ESCROW_REQUIRED`، و`cryptographicAssurance:'development_adapter'`
  مثبّتة على كل `QuorumAction` منشأ.

### الأثر
الختم يوثّق **تسلسل الأحداث للمراجعة**، ولا يمنع التلاعب. مستخدم متحكّم في جهازه يستطيع:
تزوير موافقات نصاب غير موجودة، ختم نتائج لم تمرّ بمحكّم رئيس، أو تعديل الدرجات ثم إعادة
احتساب بصمة صحيحة شكلياً. في مسابقة بنتائج مُعلنة هذا يُبطل قيمة الختم كدليل.

### شكل الحلّ (غير منفَّذ)
1. نقل `sealResults` إلى نقطة خادمية `POST /api/results/seal`: الخادم يقرأ الدرجات من مصدره
   هو (لا من حمولة العميل)، ويوقّع بـ Ed25519 بمفتاح خادمي/HSM عبر `trustSigner()` الموجود أصلاً.
2. نقل دورة حياة النصاب (`requested → approved → ready → executed`) إلى الخادم مع هوية
   مُتحقَّقة (Firebase ID token كما في `store.ts:222`)، بحيث لا تُقبل موافقة إلا من فاعل
   مصادَق عليه ومختلف عن سابقه.
3. سجل تدقيق **ملحق-فقط (append-only)** خادمي للموافقات والختم، لا يُكتب من العميل.
4. يبقى العميل يعرض الحالة فقط؛ ما يُحسب في المتصفح لا يُعتدّ به كدليل نزاهة.

---

## 2. FairDraw بلا فصل زمني بين الالتزام والكشف — خطورة: عالية

### الدليل
- `src/lib/fairdraw.ts` — `generateFairDraw()` تُنتج في **استدعاء واحد ذرّي**:
  - البذرة: `const seed = args.seed || secureSeed()`
  - الالتزام: `const seedCommitmentHash = \`SHA256:${await sha256(seed)}\``
  - والكشف في نفس الكائن المُعاد: `seedReveal: seed`
  أي أن الالتزام والكشف يولدان معاً ويُسلَّمان معاً؛ **لا توجد مرحلة زمنية بينهما**.
- `verifyFairDrawSelection` و`verifyFairDrawPublicProof` تتحققان فقط من أن
  `sha256(seedReveal) === seedCommitmentHash` — وهذا يثبت **قابلية إعادة الإنتاج**
  ولا يثبت أن الالتزام سبق معرفة النتيجة.
- كل هذا يعمل داخل المتصفح: `secureSeed()` تستدعي `crypto.getRandomValues` على العميل،
  فالبذرة معروفة للطرف المُنشئ منذ اللحظة صفر.

### الأثر
ضمان commit‑reveal يقوم كلّه على أن الملتزِم **لا يعرف** النتيجة لحظة الالتزام. هنا
يعرفها. فالمُشغّل يستطيع توليد قرعات متعددة محلياً، ونشر الالتزام الخاص بالقرعة التي
تناسبه فقط (grinding)، ويظلّ البرهان العلني «صحيحاً» بالكامل عند التحقق.

### شكل الحلّ (غير منفَّذ)
1. الالتزام يُخزَّن خادمياً أولاً: `POST /api/fairdraw/commit` يقبل `seedCommitmentHash`
   و`constraintHash` و`poolSnapshotHash` **دون البذرة**، ويردّ بختم زمني موقّع.
2. البذرة تبقى خادمية (أو مشتقّة من مصدر عشوائية مشترك: بذرة الخادم + مساهمة المحكّم
   الرئيس + مصدر عام لاحق) ولا تُسلَّم للعميل عند الالتزام.
3. `POST /api/fairdraw/reveal` لا يكشف إلا بعد تحقّق شرط لاحق زمنياً — حضور المتسابق
   واكتمال نصاب اللجنة (وهو ما تصفه `src/lib/global-round.ts` مراحلَ
   `sealed → present → quorum → revealed` لكنه اليوم مفروض تشغيلياً لا تعمّيةً).
4. رفض أي التزام لم يُسجَّل خادمياً قبل الكشف، ليصير الالتزام المتعدّد مكشوفاً.

> ملاحظة: `src/lib/readiness.ts` (فحص `question_escrow`) يصف هذه الفجوة بدقّة بالفعل:
> «بوابة اللجنة تمنع الكشف المبكر تشغيلياً، لكن السؤال ما زال يحتاج حجزاً خادمياً».
> البند الحالي هو الوجه المكمّل: البذرة أيضاً تحتاج الحجز نفسه.

---

## 3. مفتاحا Google API — أُزيلا من الشجرة، والتدوير يبقى على المالك

### الوضع الحالي في الشجرة (سليم)
- لا يوجد أي مفتاح محفور في المصدر. `src/lib/firebase.ts` يقرأ
  `import.meta.env.VITE_FIREBASE_API_KEY` وما يقابله من متغيّرات، مع سلوك آمن عند الغياب:
  يرمي خطأً صريحاً إذا `VITE_REQUIRE_AUTH==='true'` وغاب المفتاح، وإلا يستخدم
  `'development-only-no-real-key'`.
- `firebase-applet-config.json` مُلتزَم فارغاً (`{}`) عمداً.
- `npm run secret-scan` و`npm run source-audit` يمرّان نظيفين (172 ملفاً مفحوصاً).

### ما يبقى مفتوحاً
المفتاحان ما زالا موجودين في **تاريخ git** (يمكن استخراجهما من نسخة مستنسخة كاملة):

| الالتزام | الملف | الحقل |
|---|---|---|
| `9867425` | `firebase-applet-config.json` | `apiKey` (يبدأ بـ `AIzaSyAU13…`) |
| `9d46cc2` | `firebase-applet-config.json` | `apiKey` (يبدأ بـ `AIzaSyDZPk…`) |

**حذف المفتاح من الشجرة ليس إبطالاً.** المطلوب من المالك، ولا يمكن لأي وكيل تنفيذه لأنه
يحتاج لوحة Google Cloud:

1. **تدوير/إبطال المفتاحين** في Google Cloud Console → APIs & Services → Credentials.
2. تقييد المفتاح الجديد بـ HTTP referrers وبنطاق الـAPIs المطلوبة فقط.
3. حقن المفتاح الجديد وقت البناء عبر `VITE_FIREBASE_API_KEY` (انظر `.env.example`).
4. اختيارياً — تنظيف تاريخ git (إعادة كتابة + دفع قسري) بعد التدوير؛ التدوير أولاً لأنه
   وحده ما يُبطل المفتاح فعلياً.
